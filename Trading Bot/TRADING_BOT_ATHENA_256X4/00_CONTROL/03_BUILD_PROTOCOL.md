# Build Protocol

Run:

```powershell
python "self_actualize/runtime/build_trading_bot_athena_framework.py"
```

The generator reads the atlas, Trading Bot code, reports, and memory extracts, then writes the full framework shell.
