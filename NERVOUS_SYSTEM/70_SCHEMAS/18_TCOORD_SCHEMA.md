# TCoord Schema v4.3

## Purpose

`TCoord` is the higher-dimensional overlay coordinate bundle used in v4.3 routing.

## Canonical Tuple

`TCoord(Target) = (Z_i, omega, alpha, rho, nu, L, f, t)`

## Compatibility

`TCoord` augments but does not replace `LocalAddr`.
