# Autonomy Kernel Charter

Date: `2026-03-13`
Docs gate: `BLOCKED`
Priority: `control-plane first`
Scheduler role: `advisory-only`
Verdict: `IN PROGRESS`

## Governing Equation

`AutonomyKernel = AuthorityStack + AutomationOwnership + MetaLaneDecision + RepairTickets + PromotionGate + ValidationWaves + RestartLaw`

## Frozen Truth Bundle

1. Local-first evidence remains canonical until authenticated Google Docs search succeeds.
2. The canonical authority stack is `NERVOUS_SYSTEM -> self_actualize/mycelium_brain -> ECOSYSTEM/NERVOUS_SYSTEM -> 14_DEEPER...`.
3. Historical deep roots remain readable lineage mirrors, never peer authorities.
4. Hall and Temple stay macro-sized coordination organs, not literal expansion sinks.
5. One count regime must be frozen per pass before synthesis or promotion.
6. Each frontier family gets one active owner lane and zero competing writeback lanes.
7. Scheduler packets supply timing pressure only; witness, leverage, replay, and blocker honesty decide promotion.
8. Blocked live Docs state is a normal external dependency and must never be polished into symbolic success.

## Canonical Meta Lanes

| Lane | Role | Cadence | Primary job |
| --- | --- | --- | --- |
| `guildmaster-loop` | intake governor | hourly | read packets, choose one Hall frontier and one Temple pressure, land one receipt |
| `corpus-weave` | integration governor | hourly | refresh atlas, family ranking, neglected bridges, and runtime-linked witness on the chosen frontier |
| `qshrink-shiva` | compression governor | every 4 hours | compress duplicate narration and stale residue without erasing frontier truth |
| `athena-weave-scan` | expansion governor | every 12 hours | surface exactly one neglected body or archive root and bind it to one basis document and one lens |
| `high-priest-totality` | closure governor | every 24 hours | contract the last day into `OMEGA`, `ALPHA+`, and one next-day frontier stack |

The six cultural scheduler lanes remain packet-only surfaces.

## Shared Contract Surfaces

- `META_LANE_DECISION_CONTRACT.md`
- `AUTOMATION_OWNERSHIP_REGISTRY.md`
- `REPAIR_TICKET_QUEUE.md`
- `PROMOTION_GATE_CONTRACT.md`
- `VALIDATION_QUEUE.md`

## 24-Hour Success Condition

The autonomy kernel becomes locally live only when one 24-hour cycle produces:

1. one blocker-honest frontier story
2. one replay-safe receipt chain
3. one agreed active frontier tuple across Hall, Temple, queue, manifests, receipts, and automation reports
4. one lawful restart seed with a named owner lane, target artifact, and writeback destination
5. no fake live-doc claims and no duplicate owner lanes

## First-Wave Targets

1. keep duplicate control explicit across `.codex/automations`
2. route blocked fronts and failed loops into repair tickets instead of narrative residue
3. batch `V-001` through `V-038` into execution waves instead of a flat wall of pending work
4. reduce cohesion-audit missing references from `76` toward `< 40`
5. canonicalize duplicate archive-root identities in `MATH` as one SHA => one canonical root
6. keep `Stoicheia` and `CLEAN` reserve-thin until witness density changes honestly
