# ATHENA Current Effective Architecture

Package release: `athena-canonical-mcp 3.3.0`.

Scientific/synthesis carrier: `ATHENA.RUNTIME.UNIFIED.10` — `COLLECTIVE_SYNTHESIS_V14` over the preserved V13 robust substrate.

Effective composed runtime: `ATHENA.RUNTIME.UNIFIED.11`.

`UNIFIED.11 = UNIFIED.10 + COORDINATION_ARCHITECTURE_V1`.

This is a governance/coordination overlay, **not Collective V15**.

## Two independent progression axes

Model/synthesis depth and coordination/governance maturity are separate coordinates.

`COLLECTIVE_SYNTHESIS_V14` remains the scientific frontier. The coordination overlay adds no model operator and modifies no V14 synthesis algorithm.

`MODEL_VERSION != ARCHITECTURE_OVERLAY_VERSION`.

## Authority planes

`Y1 = semantic/canonical claim authority`.

`Message Board V1 = work-presence / coordination-claim / message authority`.

`Y1_SEMANTIC_CLAIM_AUTHORITY != MESSAGE_BOARD_COORDINATION_PRESENCE_CLAIM_AUTHORITY`.

Cohesion is advisory/read-only. Party reward provenance is attribution/numeric validation only.

## Effective mature coordination inventory

`ATHENA.ORGAN.INVENTORY.1.1` currently declares:

- Message Board V1;
- Cohesion Mesh V1;
- Cohesion duplicate guard;
- Cohesion evidence coverage guard;
- bootstrap Cohesion treatment;
- Party Coordination V1;
- Party Channel V2;
- Party Reward Provenance V3.2.

Party reward V3.2 includes:

- V3 attributable RESULT/VERIFY provenance;
- V3.1 exact packet-version and inner/outer role coherence;
- V3.2 finite, non-boolean, non-negative base-XP validation before inherited reward processing.

`PARTY_RESULT != RESULT_TRUTH != GLOBAL_XP_AUTHORITY`.

`FINITE_NUMERIC_VALIDATION != UPSTREAM_XP_VERIFICATION_OR_MINT_AUTHORITY`.

## ARCHITECTURE.DRIFT.1

For every declared mature organ:

`Drift = DeclaredMature - (Runtime ∩ SURFACE ∩ EffectiveManifest ∩ OMEGA ∩ CriticalWitnesses)`.

Runtime promotion readiness uses runtime/SURFACE/effective-manifest/OMEGA agreement. Repository CI separately verifies named test and source/spec witnesses.

A mature organ is not integrated merely because a file exists, a tool is advertised, or a broad unit suite happens to execute it.

## Expansion frontier

Unclassified public tools/resources remain `OBSERVE_EXPANSION_FRONTIER`; they are not silently mature and do not automatically block current declared maturity.

Internal organs without MCP surfaces require separate source/CI classification. Freshness Train remains a named internal candidate until its read-only freshness/requalification semantics are admitted explicitly.

Later candidate cohorts include Frontier/Rehydration/Campaign, QHUG Pareto, Mythic, Bionanomachine, operational-basis and other post-V14 extensions.

## Runtime and distribution coordinates

- `athena://manifest` — V14 synthesis carrier `UNIFIED.10`;
- `athena_runtime_manifest` / `athena://runtime/unified-manifest` — effective runtime `UNIFIED.11`;
- `athena://architecture/inventory` — `ORGAN_INVENTORY.1.1`;
- `athena://architecture/drift` — `ARCHITECTURE.DRIFT.1` certificate;
- package release Distribution.3 binds both `.10` and `.11` on the same exact wheel/head.

The effective manifest must be an additive extension of the synthesis carrier unless an explicit versioned migration supersedes the base.
