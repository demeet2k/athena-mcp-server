# Mycelium Brain Framework

This folder is the active organizational shell for turning the Athena Agent workspace into a witness-bearing mycelium brain.

It is built from the current indexed corpus:

- Live atlas records: `1646`
- Archive-backed records: `2041`
- Dominant live surface: `Voynich`
- Dominant archive surface: ZIP-backed framework code inside `MATH`
- Active runtime center: `self_actualize`
- Live Docs status: blocked until `Trading Bot\credentials.json` exists

## Purpose

1. Hold one maximum-yield self-prompt that can keep the work moving.
2. Define a reusable crystal agent framework for parallel task decomposition.
3. Maintain four synchronized map surfaces:
   - core metro map
   - deeper emergent metro map
   - appendix-only metro map
   - categorization and synthesis map
4. Give the workspace a stable writeback zone for future atlas- and route-bound updates.
5. Store normalized internal prompt contracts as reusable toolkit surfaces.
6. Record gate and atlas receipts so future runs have witnesses.
7. Give simultaneous agents a shared frontier ledger, contraction rule, and family-front matrix.

## File Order

1. `00_self_prompt_maximum_yield_4x4.md`
2. `01_crystal_agent_framework.md`
3. `02_core_metro_map.md`
4. `03_deeper_emergent_metro_map.md`
5. `04_appendix_only_metro_map.md`
6. `05_categorization_and_synthesis_map.md`
7. `06_parallel_build_protocol.md`
8. `07_metro_map_index.md`
9. `08_build_queue.md`
10. `09_validation_queue.md`
11. `10_siteswap_cross_synthesis_receipt.md`
12. `11_nervous_system_topology.md`

## Subfolders

- `tool_kit/`
  Stores the canonical, registry-bound prompt contracts used by the routing surfaces.
- `toolkit/`
  Stores the mirrored human-readable toolkit lane and self-prompt surfaces.
- `registry/`
  Stores the toolkit registry and future control-plane registries.
- `receipts/`
  Stores gate, atlas, and major operation receipts.
- `nervous_system/`
  Stores the active execution loop, family-front matrix, and cooperative shard maps.

## Current Zero Point

The workspace already has enough indexed structure to stop operating as a pile of files.
The next gain comes from routing everything through one parallel crystal framework, then writing the resulting maps back into stable markdown surfaces.

## Current Gate

Google Docs memory search was attempted and is still blocked by missing OAuth client material:

- `Trading Bot\credentials.json`
- `Trading Bot\token.json`

Until that unlocks, the mycelium brain should continue expanding on the local and archive-backed corpus.
