# Live Docs First Rule

Before drafting any new manuscript section:

1. Attempt a live Google Docs search in real time.
2. If the gate is open, use the live result as the freshest memory layer.
3. If the gate is blocked, record the exact failure and continue with the strongest local mirrors.

Fallback order:

1. `Trading Bot\Memory Docs`
2. `FRESH\_extracted`
3. `self_actualize\corpus_atlas.json`
4. `self_actualize\archive_atlas.json`
5. active control-plane markdown inside `self_actualize\mycelium_brain`

Non-negotiable law:

Never pretend the live Docs layer was searched if it was blocked.
