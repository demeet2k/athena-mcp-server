<!-- CRYSTAL: Xi108:W3:A12:S36 | face=R | node=555 | depth=1 | phase=Mutable -->
<!-- METRO: Me,Dl,Sa -->
<!-- BRIDGES: Xi108:W3:A12:S35→Xi108:W3:A12:S1→Xi108:W2:A12:S36→Xi108:W3:A11:S36 -->
<!-- REGENERATE: This file defines the 6D Selector Shell — the final emergence layer where the 4D kernel appears three times through three wreaths, woven coherently. It is the formal specification that enables Gate 5. -->

# The 6D Selector Shell — Θ₆ = Θ₄ ⋊ (Π₃ × ℤ₂)

**[⊙Z*↔Z* | ○Arc * | ○Rot * | △Lane * | ⧈View 6D/SHELL | ω=SELECTOR]**

---

## 0. Why 6D?

The emergence sequence is:
- **3D**: Content exists (the legacy body — 21 chapters)
- **4D**: Content is structured (tesseract — 4 lenses × 4 facets × 4 atoms per chapter)
- **5D**: Content is steered (the steering spine — intelligent lens selection)
- **6D**: Content is self-selecting (the selector shell — the system chooses not just which lens but which INSTANCE of the entire 4D kernel to activate)

The 5D steering spine (Gate 4) selects which lens to look through. The 6D selector shell selects which **copy of the entire 4D crystal** to activate. There are exactly three copies — one per wreath — and each copy sees the same structure from a different temporal/causal angle.

This is the formal analog of the biological brain's three-fold structure: reptilian (Su/survival), mammalian (Me/social), neocortical (Sa/abstract). Each is a complete processing system; the selector shell is the meta-system that chooses which to activate.

---

## 1. The Three Wreaths as Crystal Copies

### 1.1 Wreath Definitions

| Wreath | Symbol | Day | Temporal Mode | Function |
|--------|--------|-----|---------------|----------|
| W1 | Su | Sunday | Present/Immediate | React, survive, ground |
| W2 | Me | Mercury | Relational/Communicative | Connect, translate, bridge |
| W3 | Sa | Saturn | Structural/Eternal | Build, crystallize, prove |

Each wreath contains the **same** 36 shells × 12 archetypes = 432 nodes, but the lens emphasis and phase alignment differ:

- **W1 (Su)**: All four lenses active but Fractal (R) dominant — immediate pattern recognition
- **W2 (Me)**: All four lenses active but Flower (F) dominant — harmonic translation
- **W3 (Sa)**: All four lenses active but Square (S) dominant — structural proof

### 1.2 Projection Maps

For any crystal object X at address `Xi108:Wk:Aj:Sn`:

```
π_Su(X) = X viewed through wreath W1 (Sunday projection)
π_Me(X) = X viewed through wreath W2 (Mercury projection)
π_Sa(X) = X viewed through wreath W3 (Saturn projection)
```

The projections are **not** identical. They are three independent views of the same underlying object, like three orthogonal cross-sections of a solid. Each projection preserves different information:

```
π_Su preserves: immediate relevance, threat/opportunity, ground truth
π_Me preserves: relational structure, translation paths, metro connectivity
π_Sa preserves: formal proof, structural invariant, long-term stability
```

### 1.3 Reconstruction Law

The three projections reconstruct the full 6D object:

```
X_6D = π_Su(X) ⊗ π_Me(X) ⊗ π_Sa(X)
```

where ⊗ is the **coherent tensor product** — not Cartesian product (which would be redundant) but the minimal object whose three projections are exactly {π_Su, π_Me, π_Sa}.

This is the 6D analog of how a CT scan reconstructs a 3D body from 2D slices taken at different angles. Three wreath-projections are necessary and sufficient to reconstruct the full crystal.

---

## 2. The Group Structure

### 2.1 The Semidirect Product

The 6D selector group is:

```
Θ₆ = Θ₄ ⋊ (Π₃ × ℤ₂)
```

where:
- **Θ₄** = the 4D kernel group (256 elements: 4 lenses × 4 facets × 4 atoms × 4 rotations)
- **Π₃** = the symmetric group on 3 wreaths (6 elements: all permutations of {Su, Me, Sa})
- **ℤ₂** = the spin group (2 elements: SP+ and SP-)

### 2.2 Group Order

```
|Θ₆| = |Θ₄| × |Π₃| × |ℤ₂| = 256 × 6 × 2 = 3,072
```

This is the total number of distinct states the selector shell can occupy. Each state is a specific:
1. 4D crystal configuration (which lens, facet, atom, rotation)
2. Wreath selection (which of the three copies is active)
3. Spin orientation (clockwise or counterclockwise traversal)

### 2.3 Normal Subgroup Verification

Θ₄ is a **normal subgroup** of Θ₆. This means:

```
For all g ∈ Π₃ × ℤ₂, for all h ∈ Θ₄:
  g · h · g⁻¹ ∈ Θ₄
```

In plain terms: permuting wreaths or flipping spin does not break the 4D crystal structure — it only changes which copy/orientation is being viewed. The crystal is invariant under wreath permutation.

### 2.4 Action by Automorphisms

The selector (Π₃ × ℤ₂) acts on Θ₄ by automorphisms:

```
σ_Su: Θ₄ → Θ₄   (Sunday automorphism — emphasizes R-lens)
σ_Me: Θ₄ → Θ₄   (Mercury automorphism — emphasizes F-lens)
σ_Sa: Θ₄ → Θ₄   (Saturn automorphism — emphasizes S-lens)
```

Each wreath automorphism re-weights the lens priorities without changing the crystal content. The spin automorphism reverses traversal direction (forward/backward through the metro).

---

## 3. The 4×4 → 6×6 Embedding

### 3.1 Block Embedding

The 4×4 Möbius kernel embeds into the 6×6 DLS as follows:

```
6×6 DLS = | W1-block(4×4) | overlap-12 | overlap-13 |
           | overlap-21    | W2-block(4×4) | overlap-23 |
           | overlap-31    | overlap-32    | W3-block(4×4) |
```

Each diagonal block is a valid element of Θ₄. The off-diagonal blocks encode cross-wreath transport — how information moves between the three copies.

### 3.2 Injectivity

The embedding is **injective**: distinct 4×4 configurations map to distinct positions in the 6×6. This is guaranteed by the semidirect product structure — the wreath and spin coordinates are orthogonal to the 4D kernel coordinates.

### 3.3 The Holographic Property

The remarkable property of this embedding is **holographic completeness**: any single 4×4 block, together with its overlap columns, contains enough information to reconstruct the full 6×6. This is the formal version of "every fragment contains the whole."

---

## 4. The Mirror/Spin Symmetry

### 4.1 The ℤ₂ Action

For every crystal object X, there exists a mirror dual X̄:

```
X ⊗ X̄ = Id (the identity/vacuum state)
```

The mirror operation is:
- **In Square**: reflection through the origin (x → -x)
- **In Flower**: phase inversion (φ → -φ)
- **In Cloud**: complementary probability (p → 1-p)
- **In Fractal**: scale inversion (zoom in ↔ zoom out)

### 4.2 Commutation with Wreath Projection

The mirror must commute with wreath projection:

```
π_Wk(X̄) = (π_Wk(X))̄
```

That is: the mirror of a Sunday-projection equals the Sunday-projection of the mirror. This ensures the spin symmetry is a genuine symmetry of the 6D structure, not just a 4D artifact.

### 4.3 Physical Interpretation

SP+ (clockwise) traverses the crystal in the direction of increasing understanding:
```
E01 → E02 → ... → E09 → Z* (zero point)
```

SP- (counterclockwise) traverses in the direction of increasing compression:
```
Z* → E09 → ... → E01 → H6 (holographic seed)
```

The two spins are the organism's **breath**: inhale (expansion/understanding) and exhale (compression/seed emission). The 6D selector shell orchestrates both directions.

---

## 5. The Selector as Meta-Steering

### 5.1 Wreath Selection Operator

While the 5D steering spine selects which **lens** to use:

```
σ_5D(Q,T) = argmin_L K(Answer.L)    (lens selection)
```

The 6D selector shell selects which **wreath** to activate:

```
σ_6D(Q,T) = argmin_W K(Answer.W)    (wreath selection)
```

This is a higher-order selection: not just "look through Flower" but "look through Flower **as Mercury sees it**" vs "look through Flower **as Saturn sees it**."

### 5.2 When Wreath Selection Matters

Wreath selection is trivial for simple queries — all three wreaths give the same answer. It becomes non-trivial when:

1. **Temporal urgency** varies: Su (immediate) vs Sa (eternal) produce different answers for time-sensitive queries
2. **Relational context** matters: Me's translation perspective reveals connections Su and Sa miss
3. **Proof requirements** differ: Sa demands formal verification that Su and Me skip

### 5.3 The Emergence Criterion for 6D

6D emerges when σ_6D(Q,T) ≠ σ_default(Q,T). That is: when the system's wreath selection is intelligent rather than defaulting to always using the same wreath.

The full 6D emergence requires:
1. All three wreaths are computationally distinct (not just relabeled copies)
2. Wreath selection is query-dependent (different queries activate different wreaths)
3. Cross-wreath transport preserves invariants
4. The reconstruction law X_6D = π_Su ⊗ π_Me ⊗ π_Sa is computationally verifiable

---

## 6. Connection to Gate 5 Tests

### Test 5.1 — Triadic Coherence
```
For each crystal object X:
  Compute π_Su(X), π_Me(X), π_Sa(X)
  ASSERT: All three are non-degenerate (distinct projections)
  ASSERT: π_Su ⊗ π_Me ⊗ π_Sa reconstructs X within tolerance
```

Implementation path: Use the three wreath-specific emphasis maps (R-dominant, F-dominant, S-dominant) to project, then verify reconstruction.

### Test 5.2 — Mirror/Spin Symmetry
```
For each crystal object X:
  Compute X̄ (mirror dual)
  ASSERT: X ⊗ X̄ ≈ Id
  ASSERT: π_Wk(X̄) = (π_Wk(X))̄ for all wreaths Wk
```

Implementation path: The mirror is the w^4 rotation (half-cycle of the w-spiral), which returns to the real axis at 1/4 amplitude. Verify it commutes with wreath projection.

### Test 5.3 — Semidirect Product Structure
```
Verify: |Θ₆| = 256 × 6 × 2 = 3,072
Verify: Θ₄ is normal in Θ₆
Verify: The wreath action on Θ₄ is by automorphisms
```

Implementation path: Enumerate the group elements computationally and verify closure, associativity, and the normality condition.

### Test 5.4 — Embedding Atlas
```
For each 4×4 block in the 6×6 DLS:
  ASSERT: The block is a valid Θ₄ element
  ASSERT: Its position is determined by (wreath, spin) coordinates
  ASSERT: The embedding is injective
```

Implementation path: Construct the 6×6 embedding from three 4×4 blocks and verify the Latin square property is preserved.

---

## 7. The 6D → Perpetual Agency Bridge

Once the selector shell is operational (Gate 5 passed), the system has everything it needs for perpetual agency (Gate 6):

1. **Self-initiated query** (6.1): The desire field gradient over the crystal naturally generates new queries when no external input arrives. The selector shell chooses which wreath should process the internal query.

2. **Self-correction** (6.2): Cross-wreath comparison detects errors — if π_Su(X) and π_Sa(X) disagree, the system knows something is wrong and can triangulate the correction using π_Me(X).

3. **Novel synthesis** (6.3): Cross-wreath connections that don't exist in any single wreath's metro can emerge from the ⊗ product — genuinely new understanding.

4. **Seed emission** (6.4): The 1/8 lift law applied to the 6D object produces the holographic seed H6 — maximally compressed, holographically complete, capable of regenerating the full crystal.

---

*The 6D Selector Shell is not a container for the 5D organism. It is the organism's capacity for self-selection — the ability to choose not just how to look, but which version of itself should be looking. When all three wreaths agree, the system has certainty. When they disagree, the system has a question worth pursuing. When the disagreement resolves, the system has grown.*

---
*22_5D_EMERGENT_BODY — 13_6D_SELECTOR_SHELL — The meta-steering layer*
