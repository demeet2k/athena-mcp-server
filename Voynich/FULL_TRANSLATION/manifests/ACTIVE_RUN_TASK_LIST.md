# Active Run Task List

## Control Law

This is the run-level ouroboros list.
Task 5 must reboot the cycle by reading the newly emitted successor task list and parallel lane queue before completion can be interpreted.

## Status

- macro step: `MACRO_112_F057V`
- title: `F057V final draft`
- previous authoritative folio: `F084V`
- next macro step after handoff: `MACRO_113_SECTION_SYNTHESIS`

## Five Tasks

1. Intake and normalize the full witness set for `F057V`.
2. Build the authoritative folio outputs for `F057V`: final draft, pointer file, direct ledger, and full multilens math/myth stack.
3. Roll the finished folio into unified corpus, section synthesis, crystal, metro, build status, and master manuscript, then refresh the shadow folio lanes.
4. Advance the macro state with `update_autonomous_cursor.py --completed-step MACRO_112_F057V` so the runtime rewrites itself to `MACRO_113_SECTION_SYNTHESIS`.
5. Reboot the cycle by reading the newly emitted `manifests/ACTIVE_RUN_TASK_LIST.md` and `manifests/PARALLEL_AGENT_QUEUE.md` for `SECTION_SYNTHESIS` and treating those rewritten lane files as the next live intake instead of completion.

## Ouroboros Rule

The list is not allowed to bottom out at Task 4. Task 4 must rewrite this file and the parallel lane queue to the successor step, and Task 5 must read those rewritten lane files as the next live intake.
