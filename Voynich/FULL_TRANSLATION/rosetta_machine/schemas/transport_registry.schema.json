{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Voynich Transport Registry Record",
  "type": "object",
  "required": [
    "id",
    "kind",
    "source_unit",
    "evidence_class",
    "confidence",
    "docs_gate",
    "inputs",
    "transform",
    "outputs",
    "constraints",
    "export_targets",
    "source_refs",
    "notation_id",
    "notation_class",
    "source_carrier",
    "target_carrier",
    "transport_law",
    "target_fields"
  ],
  "properties": {
    "id": {"type": "string"},
    "kind": {"type": "string", "enum": ["notation_transport"]},
    "source_unit": {"type": "string"},
    "evidence_class": {"type": "string", "enum": ["direct", "derived", "mixed", "uncertain"]},
    "confidence": {"type": "number", "minimum": 0, "maximum": 1},
    "docs_gate": {"type": "string", "enum": ["BLOCKED", "LIVE", "UNKNOWN"]},
    "inputs": {"type": "array"},
    "transform": {"type": "object"},
    "outputs": {"type": "array"},
    "constraints": {"type": "object"},
    "export_targets": {"type": "array", "items": {"type": "string"}},
    "source_refs": {"type": "array", "items": {"type": "string"}},
    "notation_id": {"type": "string"},
    "notation_class": {"type": "string"},
    "source_carrier": {"type": "string"},
    "target_carrier": {"type": "string"},
    "transport_law": {"type": "string"},
    "target_fields": {"type": "array", "items": {"type": "string"}}
  },
  "additionalProperties": true
}
