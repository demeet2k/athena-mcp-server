# ATHENA ARCHITECTURE V15

## Layer map

`CCR/KC144/JSPACE/SCALE -> Collective V1..V14 -> COLLECTIVE_CALIBRATED_V15 -> authority/execution/trust membranes`.

Parallel operational fibers remain active:

`PROMPT/FRONTIER`, `REHYDRATION`, `MESSAGE_BOARD/COHESION/PARTY`, `DEPLOYMENT.2`, `RELEASE`, `PROMOTION.2`.

V15 is a science/control fiber, not a replacement root.

## V15 coordinate

`COLLECTIVE_CALIBRATED=<SR,XT,XD,CJ,AT,MD,L>`.

`SR` calibrates empirical structural reliability from external correctness witnesses using duplicate-pooled weighted PAV and explicit right-continuous step interpolation.

`XT` cross-fits bounded two-timepoint sequential TMLE while preserving observed stage-2 history before stage-1 intervention.

`XD` cross-fits bounded two-timepoint sequential AIPW policy value under explicit decision-time information sets.

`CJ` performs exact finite-dimensional linear-Gaussian conditioning and linear Gaussian action-value propagation over strict declared finite coordinates.

`AT` transports witnessed approximation error under an explicit, witness-consistent Lipschitz envelope while separating nearest, global-envelope and radius-eligible local witnesses.

`MD` solves exact finite-horizon dynamic programs for supplied finite state/action rectangular total-variation ambiguity with strict finite transition coordinates.

`L` preserves native lineage, assumptions, model scope and witness coordinates.

## Fiber/base law

Scientific/calibration/control state may change while canonical identity remains fixed:

`FIBER_MOTION != BASE_CANON_MUTATION`.

V15 read/design/control operators therefore have no independent authority to mutate Y1, JSPACE, empirical observation history, deployment history or trusted promotion state.

## Calibration coordinate geometry

Externally labelled structural examples may repeat the same support coordinate. Those rows are aggregated before PAV:

`W_x=sum_i w_i`, `S_x=sum_i w_i y_i`, `p_x=S_x/W_x`.

The fitted monotone block value is therefore a function of the semantic support coordinate, not row order.

`IDENTICAL_CALIBRATION_COORDINATE != MULTIPLE_FITTED_VALUES`.

Between knots, the fitted value at the most recent support is carried forward until the next knot; endpoint values extend outside the observed range.

`RIGHT_CONTINUOUS_LABEL == RIGHT_CONTINUOUS_EXECUTABLE_PATH` is regression-tested rather than assumed.

## Cross-fitting and temporal-information chart

For folds `D_1..D_K`, each fold is evaluated using nuisance/target models trained on `D\D_k`. The aggregation surface records that cross-fitting occurred but does not infer causal identification from it.

`CROSS_FITTING != IDENTIFICATION`.

Longitudinal history is not the full row. V15 makes the legal policy information sets explicit:

`I_1 = baseline`

`I_2 = baseline union {A1,L1}`.

Thus stage-1 policy logic cannot read `L1`, `A2` or `Y`, and stage-2 policy logic cannot read `A2` or `Y`.

`DECISION_TIME_HISTORY != FULL_ROW_STATE`.

The TMLE stage-2 pseudo-outcome also preserves observed `A1,L1` before any stage-1 intervention:

`STAGE2_PSEUDO_OUTCOME_PRESERVES_OBSERVED_A1_L1_BEFORE_STAGE1_INTERVENTION`.

## Continuous-belief chart

Current continuous joint belief is deliberately narrow: finite-dimensional multivariate Gaussian state plus linear Gaussian observations. This exact tractable chart is preferred over falsely naming a general continuous posterior.

Unknown coefficient coordinates are rejected rather than projected to zero. Non-finite numeric state is not accepted as a model coordinate.

`UNKNOWN_COEFFICIENT != ZERO_COEFFICIENT`.

`NONFINITE_NUMERIC_STATE != MODEL_COORDINATE`.

## Approximation-error transport chart

For witness error `e_i` at `x_i` and declared Lipschitz bound `L`, the unrestricted envelope is

`e_global(x) <= min_i [e_i + L ||x-x_i||]`.

But three navigations are distinct:

`N(x)=argmin_i ||x-x_i||`

`G(x)=argmin_i [e_i+L||x-x_i||]`

`R_r(x)=argmin_{i:||x-x_i||<=r}[e_i+L||x-x_i||]`.

`N`, `G`, and `R_r` need not coincide.

`GEOMETRIC_NEAREST_WITNESS != TIGHTEST_ERROR_ENVELOPE_WITNESS`.

`GLOBAL_ENVELOPE != RADIUS_ELIGIBLE_LOCAL_CERTIFICATE`.

## Robust-control chart

Current multistage robust control is exact only for finite horizon, finite states/actions and state-action rectangular TV ambiguity. This is a true Bellman recursion within its declared model, not general DRO.

Unknown state coordinates, duplicate action identities, non-finite rewards/transitions and incomplete probability models fail closed.

`UNKNOWN_STATE_COORDINATE != UNUSED_METADATA`.

`NONFINITE_TRANSITION != PROBABILITY_MODEL`.

## Release-overlay projection graph

V15 integration established that a semantic release coordinate can project into multiple Python objects:

`protocol.SERVER_INFO -> dispatch.SERVER_INFO`

`UNIFIED_MANIFEST_RESOURCES -> INTEGRITY_RESOURCES -> AOR_DEVELOPMENT_RESOURCES`

`unified_manifest.build_unified_manifest -> dispatch.build_unified_manifest`.

Import/copy edges are not guaranteed aliases. Therefore an overlay is complete only when every live authority-bearing projection is migrated or explicitly typed historical/independent.

`MODULE_ATTRIBUTE_ADVANCE != IMPORTED_VALUE_SNAPSHOT_ADVANCE`.

`MODULE_FUNCTION_REPLACEMENT != IMPORTED_FUNCTION_SNAPSHOT_ADVANCE`.

`SOURCE_LIST_MUTATION != COPIED_RESOURCE_REGISTRY_MUTATION`.

Manifest holonomy is executable: tool manifest, runtime-manifest resource and canonical manifest resource must resolve to the same current artifact/layers/V15 coordinate/invariants.

## Proof-selection chart

A command can exit zero while selecting no tests. V3.4 therefore resolves every critical `unittest discover -p` selector against an actual repository file.

`ZERO_TEST_SELECTION != PROOF`.

`TEST_COMMAND_EXIT_0 != WITNESSED_ASSERTION_EXECUTION`.

## Composition invariants

`COLLECTIVE_CALIBRATED != DEPLOYMENT_AUTHORITY != COORDINATION_AUTHORITY`.

`OUT_OF_FOLD_RELIABILITY != CAUSAL_GRAPH_POSTERIOR`.

`CROSS_FITTED_ESTIMATE != TREATMENT_AUTHORIZATION`.

`GAUSSIAN_POSTERIOR != WORLD_TRUTH`.

`TRANSPORT_ENVELOPE != GLOBAL_EMPIRICAL_ERROR_MODEL`.

`ROBUST_MODEL_CERTIFICATE != REAL_WORLD_GUARANTEE`.

`MODEL_REPAIR != EPISTEMIC_PROMOTION`.
