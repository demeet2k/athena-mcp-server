# ATHENA UNIFIED V15 — UNIFIED.11 COMPOSITION

Current candidate identity:

- package: `athena-canonical-mcp@3.4.0`
- architecture: `ATHENA.RUNTIME.UNIFIED.11`
- Collective frontier: `COLLECTIVE_CALIBRATED_V15`
- coordinate: `COLLECTIVE_CALIBRATED=<SR,XT,XD,CJ,AT,MD,L>`

V15 is composed into the same `Server`; it does not create a second runtime root.

## Whole-organism braid

The current organism contains more than the Collective ladder. V15 must coexist with the inherited prompt/frontier runtime, rehydration/successor/handoff, Message Board/Cohesion, Party/Campaign/Life/Game-Time, QHUG, mythic/bionano/symbolic kernels, Deployment.2, release distribution and host-bound promotion verification.

`COLLECTIVE_CALIBRATED != WHOLE_ORGANISM`.

`COLLECTIVE_CALIBRATED != DEPLOYMENT_AUTHORITY`.

`MODEL_STATE != COORDINATION_AUTHORITY`.

## Successor topology

`Collective(V1-V14) -> COLLECTIVE_CALIBRATED_V15` is a successor extension. V15 tools are registered through the V14 protocol surface and are routed first by the V13 successor dispatcher so transitive tool inheritance cannot accidentally send a V15 RPC to the V14 implementation.

V15 resource:

`athena://collective/v15`.

SURFACE.2 owns a distinct `collective_v15` group so V14 and V15 cannot silently claim the same tools.

## V15 internal coordinate membrane

The V15 coordinate is not only a list of seven tools; each organ has a declared information/numerical geometry:

`SR` canonicalizes duplicate support coordinates before weighted PAV and exposes an explicit right-continuous step mapping.

`XT` uses out-of-fold nuisance/target models and preserves observed `A1,L1` at stage 2 before stage-1 intervention.

`XD` binds policy information to decision time:

`I1=baseline`, `I2=baseline∪{A1,L1}`.

`CJ` accepts only finite declared Gaussian coordinates; unknown coefficient keys are errors rather than implicit zeroes.

`AT` keeps geometric nearest witness, unrestricted global error envelope and radius-eligible local certificate distinct.

`MD` accepts only finite declared state/action/transition coordinates and exact normalized finite transition distributions.

These membranes are part of the release contract:

`IDENTICAL_CALIBRATION_COORDINATE != MULTIPLE_FITTED_VALUES`.

`DECISION_TIME_HISTORY != FULL_ROW_STATE`.

`UNKNOWN_COEFFICIENT != ZERO_COEFFICIENT`.

`NONFINITE_NUMERIC_STATE != MODEL_COORDINATE`.

`GEOMETRIC_NEAREST_WITNESS != TIGHTEST_ERROR_ENVELOPE_WITNESS`.

`GLOBAL_ENVELOPE != RADIUS_ELIGIBLE_LOCAL_CERTIFICATE`.

`UNKNOWN_STATE_COORDINATE != UNUSED_METADATA`.

`NONFINITE_TRANSITION != PROBABILITY_MODEL`.

## Deployment.2 braid

Live master evolved Deployment.2 while Ω15 was in flight. Ω15 therefore braids the exact deployment head as a Git parent and preserves deployment installation before the final V15 release overlay:

`V14 -> Deployment.2 -> V15 release overlay`.

Deployment remains separately typed as `DEPLOYMENT_EXTERNAL_CONTROL` and its tools/resources are retained.

## Release-overlay holonomy

V15 integration exposed several import-time snapshots of the same semantic release coordinate:

- `SERVER_INFO` imported by value into dispatch;
- resource lists copied into RuntimeIntegritySurface and then AorDevelopmentSurface;
- `build_unified_manifest` and `maxdev_law` imported by value into dispatch.

Therefore a module-level version change is not sufficient. The V15 release overlay migrates each live authority-bearing projection explicitly.

`RELEASE_ATTRIBUTE_UPDATE != IMPORTED_VALUE_SNAPSHOT_UPDATE`.

`PROTOCOL_RESOURCE_ADVANCE != LIVE_INTEGRITY_RESOURCE_ADVANCE`.

`INTEGRITY_RESOURCE_ADVANCE != COPIED_AOR_RESOURCE_ADVANCE`.

`MODULE_ATTRIBUTE_ADVANCE != IMPORTED_FUNCTION_SNAPSHOT_ADVANCE`.

Public manifest holonomy is regression-tested across:

`athena_runtime_manifest`

`athena://runtime/unified-manifest`

`athena://manifest`.

All three current paths must agree on the same artifact, layer set, V15 coordinate payload and invariant set.

`MULTIPLE_EQUIVALENT_ARCHITECTURE_CHARTS_REQUIRE_ALL_DECLARED_CHARTS_TO_MIGRATE`.

## Proof-selection membrane

V3.4 critical checks validate their own test selectors. A `unittest discover -p` command that selects zero files cannot count as a witness merely because the process exits zero.

`ZERO_TEST_SELECTION != PROOF`.

`TEST_COMMAND_EXIT_0 != WITNESSED_ASSERTION_EXECUTION`.

## Trust

The host-bound `GITHUB_PROMOTION_VERIFIER.1` remains unchanged. Caller-visible MCP arguments cannot select repository/API/run/token/trusted app/required checks. A V15 head receives `QUALIFIED` only from a real coherent exact-head GitHub Actions receipt after syntax, unit, critical-invariants and smoke pass.

`VERIFIER_IMPLEMENTED != HEAD_QUALIFIED`.

## Architecture chart migration

Every current chart must advance together:

`ATHENA.RUNTIME.UNIFIED.10 -> ATHENA.RUNTIME.UNIFIED.11`

`Collective(V1-V14) -> Collective(V1-V15)`

`COLLECTIVE_SYNTHESIS_V14 -> COLLECTIVE_CALIBRATED_V15`.

Historical V3.3 publication remains separately typed and immutable; it does not become current V3.4 release evidence.

`HISTORICAL_PUBLICATION_AUTHORITY != CURRENT_RELEASE_IDENTITY`.
