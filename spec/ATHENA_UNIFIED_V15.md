# ATHENA UNIFIED V15 — UNIFIED.11 COMPOSITION

Current candidate identity:

- package: `athena-canonical-mcp@3.4.0`
- architecture: `ATHENA.RUNTIME.UNIFIED.11`
- Collective frontier: `COLLECTIVE_CALIBRATED_V15`
- coordinate: `COLLECTIVE_CALIBRATED=<SR,XT,XD,CJ,AT,MD,L>`

V15 is composed into the same `Server`; it does not create a second runtime root.

## Whole-organism braid

The current organism contains more than the Collective ladder. V15 must coexist with the inherited prompt/frontier runtime, rehydration/successor/handoff, Message Board/Cohesion, Party/Campaign/Life/Game-Time, QHUG, mythic/bionano/symbolic kernels, Deployment.2, release distribution and host-bound promotion verification.

`COLLECTIVE_CALIBRATED != WHOLE_ORGANISM`.

`COLLECTIVE_CALIBRATED != DEPLOYMENT_AUTHORITY`.

`MODEL_STATE != COORDINATION_AUTHORITY`.

## Successor topology

`Collective(V1-V14) -> COLLECTIVE_CALIBRATED_V15` is a successor extension. V15 tools are registered through the V14 protocol surface and are routed first by the V13 successor dispatcher so transitive tool inheritance cannot accidentally send a V15 RPC to the V14 implementation.

V15 resource:

`athena://collective/v15`.

SURFACE.2 owns a distinct `collective_v15` group so V14 and V15 cannot silently claim the same tools.

## Deployment.2 braid

Live master evolved Deployment.2 while Ω15 was in flight. Ω15 therefore braids the exact deployment head as a Git parent and preserves deployment installation before the final V15 release overlay:

`V14 -> Deployment.2 -> V15 release overlay`.

Deployment remains separately typed as `DEPLOYMENT_EXTERNAL_CONTROL` and its tools/resources are retained.

## Trust

The host-bound `GITHUB_PROMOTION_VERIFIER.1` remains unchanged. Caller-visible MCP arguments cannot select repository/API/run/token/trusted app/required checks. A V15 head receives `QUALIFIED` only from a real coherent exact-head GitHub Actions receipt after syntax, unit, critical-invariants and smoke pass.

`VERIFIER_IMPLEMENTED != HEAD_QUALIFIED`.

## Architecture chart migration

Every current chart must advance together:

`ATHENA.RUNTIME.UNIFIED.10 -> ATHENA.RUNTIME.UNIFIED.11`

`Collective(V1-V14) -> Collective(V1-V15)`

`COLLECTIVE_SYNTHESIS_V14 -> COLLECTIVE_CALIBRATED_V15`.

`MULTIPLE_EQUIVALENT_ARCHITECTURE_CHARTS_REQUIRE_ALL_DECLARED_CHARTS_TO_MIGRATE`.
