# ATHENA UNIFIED V14 — AΩR × COLLECTIVE SYNTHESIS × HOST-BOUND QUALIFICATION

Package target: `athena-canonical-mcp 3.3.0`.

Live architecture target: `ATHENA.RUNTIME.UNIFIED.10`.

Current Collective frontier:

`COLLECTIVE_SYNTHESIS=<JB,SE,JE,DR,RP,AZ,MR,L>`.

## One organism, two developmental axes

The post-Ω13 runtime contains substantially more than the Collective ladder. The current package preserves prompt runtime/frontier, rehydration/successor/handoff/terminal logic, Message Board/cohesion coordination, campaign/life-loop/game-time mechanisms, QHUG Pareto and bionanomachine organs, state foundation, release distribution, and host-bound GitHub promotion verification.

V14 is therefore an additive science/control organ, not a replacement root runtime.

`AOR = WHAT is eligible`.

`COLLECTIVE = HOW bounded scientific/control capacity reasons and allocates`.

`Y1 = canonical claim authority`.

`PROMOTION.2 + GITHUB_PROMOTION_VERIFIER.1 = exact-head repository qualification`.

Operational/game/coordination organs remain separately typed.

## Identity and authority firewalls

`IDENTITY != COORDINATE != MODEL != EVIDENCE != AUTHORITY != EXECUTION != OBSERVATION != PERSISTENCE != ATTESTATION != TRUSTED_VERIFICATION`.

Canonical authority RPCs remain `athena_claim_*`.

Science-shadow discovery/replication RPCs remain `athena_discovery_claim_*`.

`athena_claim_* != athena_discovery_claim_*`.

V14 adds no implicit bridge from science-twin state into Y1.

## V14 finite joint science twin

`athena_joint_factor_belief` constructs a bounded factor-product belief from explicit axes, compatibility and optional likelihoods.

`FINITE_FACTOR_PRODUCT_BELIEF != FULL_JOINT_POSTERIOR`.

The product is bounded at 512 states so exact enumeration remains visible and replayable.

## V14 structural uncertainty

`athena_structural_bootstrap_ensemble` repeatedly executes V13 FCI-lite over bootstrap row samples.

`BOOTSTRAP_GRAPH_FREQUENCY != CAUSAL_POSTERIOR`.

No bootstrap graph state mutates JSPACE.

## V14 joint experimental value

`athena_joint_science_evi` evaluates one experiment against the joint finite science-twin state and exposes both:

`DecisionEVI`

and

`JointInformationGain`.

These remain separate before scalar weighting.

`JOINT_SCIENCE_EVI != OBSERVATION_OR_EVIDENCE`.

## V14 longitudinal policy value

`athena_sequential_dr_policy_value` extends V13 temporal-history discipline into a two-timepoint sequential AIPW score for deterministic dynamic policies.

`STAGE2_POLICY_PSEUDO_OUTCOME_PRESERVES_OBSERVED_A1_L1_BEFORE_STAGE1_POLICY_EVALUATION`.

`cross_fitted=false` is explicit.

`SEQUENTIAL_DR_POLICY_VALUE != GENERAL_LONGITUDINAL_CAUSAL_VALUE_THEOREM`.

Declared latent confounding fails closed.

## V14 robust policy geometry

`athena_joint_policy_robust` preserves expected utility, lower-tail CVaR, worst-case utility, expected/max regret, cost, and Pareto alternatives over the supplied weighted joint states.

`FINITE_SCENARIO_ROBUST_POLICY != GENERAL_ROBUST_CONTROL`.

`POLICY_PLAN != EXECUTION_AUTHORIZATION`.

## V14 adaptive zoom

`athena_gp_resolution_route` turns V13 FITC reference error into a decision-relative routing law.

The shallowest tested approximation is eligible only when it preserves the exact current action winner and its maximum witnessed utility error is below a declared fraction of the exact decision margin.

`QUERY_SET_DECISION_PRESERVATION != GLOBAL_APPROXIMATION_CERTIFICATE`.

This is a concrete computational interpretation of an adaptive KC/BR zoom family: choose the least expensive tested representation sufficient for the current decision, not the deepest available representation by default.

## V14 finite recourse

`athena_two_stage_resource_plan` supports exact small first-stage subset enumeration plus scenario-dependent one-option recourse.

`FINITE_TWO_STAGE_SCENARIO_RECOURSE != GENERAL_MULTISTAGE_STOCHASTIC_PROGRAM`.

Exactness is scoped to the supplied finite scenarios, deterministic resource packets, bounded recourse set and enumerated first-stage search.

## Surface / resource ABI

V14 tools are exposed through the existing V12→V13 successor membrane. `SURFACE.2` gives V14 a dedicated group rather than attributing the transitive tool names to V13.

Resource:

`athena://collective/v14`.

It reports `COLLECTIVE_RUNTIME_V14`, the synthesis coordinate, current laws, claim namespace and the no-laundering boundary.

## Runtime architecture ABI

Current artifact:

`ATHENA.RUNTIME.UNIFIED.10`.

Compatibility retains `.1` through `.9`.

`COLLECTIVE_SYNTHESIS_V14` is inserted above `COLLECTIVE_ROBUST_V13`; `GITHUB_PROMOTION_VERIFIER.1` remains a separate trust organ.

## Trusted qualification

Readiness remains:

`syntax ∧ unit ∧ critical-invariants ∧ smoke`.

Trusted qualification remains the fifth dependent stage:

`readiness -> promotion-qualification`.

The GitHub verifier accepts only the target Git SHA from the caller. Repository/API/run/token/trusted-app/required-check configuration remains host-bound.

`CALLER_ATTESTATION != TRUSTED_EXTERNAL_VERIFICATION`.

`ATTESTED_READY != QUALIFIED`.

`CHECKS_FROM_DIFFERENT_SUITES_OR_RUNS != ONE_TRUSTED_QUALIFICATION`.

V14 does not weaken those constraints.

## Release boundary

A trusted release receipt certifies exact repository/package/distribution state for one exact head. It is not empirical truth, causal proof, treatment authorization, production deployment, Y1 authority, or GitHub administrative hardening.

## Residual frontier

The next frontier is calibrated structural posterior inference, cross-fitted arbitrary-horizon longitudinal targeting, continuous joint belief/control, approximation-error transport beyond witnessed query sets, and genuinely multistage stochastic/distributionally robust control.
