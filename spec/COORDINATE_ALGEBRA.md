# Coordinate algebra
For `g∈{1,…,144}`: `row=1+floor((g-1)/12)`, `col=1+((g-1) mod 12)`. Token coordinate `K(x_k)=⟨SID_g,OID,VID,MID,P,S,T,[char_start,char_end)⟩`. The coordinate indexes the native manifestation; it never replaces it.
