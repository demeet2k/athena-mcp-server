# Prompt Runtime V1 Status

Repository-level specification is present on master. Executable tool integration is developed separately and should merge only after tests pass.

This file intentionally distinguishes:

`SPECIFIED != IMPLEMENTED != TESTED != MERGED`.