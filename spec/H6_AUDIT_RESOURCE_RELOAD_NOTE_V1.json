{
  "artifact":"ATHENA.H6.AUDIT.RESOURCE.RELOAD.NOTE.V1",
  "note":"Prior connector response resource was no longer readable; source audit must re-fetch exact current files rather than rely on expired response handles."
}
