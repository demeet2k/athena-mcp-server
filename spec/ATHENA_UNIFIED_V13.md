# ATHENA UNIFIED V13 — AΩR × COLLECTIVE ROBUST CONTINUOUS-BAYES / CAUSAL-CONTROL RUNTIME

Package target: `athena-canonical-mcp 3.2.0`

Live architecture artifact: `ATHENA.RUNTIME.UNIFIED.8`

Runtime root: one composed `Server`.

Canonical coordinate:

`ATHENA_UNIFIED_V13=<CCR,JSPACE,SCALE,KC144,AOR,Y1,EQ,SX,RAG,HUG,GAP,FIELD,TRANSPORT,CYCLE,SCHEMA,OMEGA,RECON,SELFTEST,STARTUP,SURFACE,COMPOSITION,PROMOTION2,COLLECTIVE(V1-V13),FINAL>`

V13 robust coordinate:

`COLLECTIVE_ROBUST=<HB,FG,JD,FC,LT,DP,DR,L>`.

## 1. Constitutional separation

`IDENTITY != COORDINATE != MODEL != EVIDENCE != AUTHORITY != EXECUTION != OBSERVATION != PERSISTENCE != ATTESTATION != TRUSTED_VERIFICATION`.

`UNKNOWN != 0`.

`KNOWN != COMPARABLE`.

`CONSENSUS != EVIDENCE`.

`PLAN / SIMULATION / DESIGN != EXECUTION / OBSERVATION / RESULT`.

`CALLER_ATTESTATION != TRUSTED_EXTERNAL_VERIFICATION`.

`ATTESTED_READY != QUALIFIED`.

Canonical Y1 authority RPCs remain `athena_claim_*`.

Science-shadow discovery/replication/falsification RPCs remain `athena_discovery_claim_*`.

`athena_claim_* != athena_discovery_claim_*`.

Collective V13 adds inference/control expressiveness; it does not gain Y1, semantic, Git, topology or trusted-promotion authority by numerical sophistication.

## 2. One server, resident and lazy organs

Resident substrate remains base CCR/JSPACE/SCALE/KC144/polycoordinate/crystal/Git, Collective V1–V4, AOR/Y1/EQ/developmental organs and state/governance organs.

V5–V13 remain lazy science/inference/control layers instantiated through tool/resource access.

`LAZY != ABSENT`.

`LAZY != RESIDENT_DUPLICATE_RUNTIME`.

SURFACE.2 requires every V13 tool plus `athena://collective/v13`. COMPOSITION.2 verifies one runtime rather than a successor subclass stack.

## 3. Developmental cycle

`HYDRATE -> RECONRUN/OMEGA -> MEMORY -> EXTRACT -> RETRIEVE -> HUG -> GAP -> FIELD -> MEASURE/CALIBRATE -> Y1/AOR -> COLLECTIVE(V1-V13) -> EXECUTE -> VERIFY -> LEARN -> SUCCESSOR -> COMPLETE`.

Missing semantic prerequisites produce typed `WAITING_*` states. No model, optimizer or planner may invent an executor, witness, observation or authority transition.

## 4. V13 continuous-domain hyperbelief

`athena_gp_hyperqmc` uses deterministic Halton particles in positive log-hyperparameter space and exact bounded GP marginal-likelihood scoring to approximate a continuous-domain posterior under a visible log-uniform prior box.

`QMC_CONTINUOUS_HYPERPOSTERIOR != EXACT_CONTINUOUS_HYPERPARAMETER_BAYES`.

Finite quadrature density, low entropy or one dominant particle does not establish the true kernel.

## 5. V13 inducing approximation

`athena_gp_fitc_predict` uses deterministic farthest-point inducing rows and a FITC diagonal conditional correction. It exposes exact-current-GP query error and a complexity proxy.

`FITC_INDUCING_GP != FULL_GP_POSTERIOR_OR_VARIATIONAL_OPTIMUM`.

`QUERY_REFERENCE_ERROR != GLOBAL_APPROXIMATION_GUARANTEE`.

## 6. V13 joint information / decision design

`athena_gp_joint_design` asks both:

- how much could a candidate measurement change the best downstream action?;
- how much could it discriminate the current continuous-domain hypermodel approximation?

The experimental crystal therefore contains decision EVSI and model-information gain separately before explicit scalar weighting.

`JOINT_MODEL_INFORMATION_DESIGN != OBSERVATION`.

Hypothetical measurements, model weights and action updates never self-train.

## 7. V13 structural discovery

`athena_fci_lite_discover` expands V12's bounded PAG candidate by conditioning on all observed-variable subsets up to a declared small order, retaining separation sets, unshielded collider marks and limited orientation propagation.

`BOUNDED_FCI_LITE != FCI_RFCI_PAG_THEOREM`.

Complete possible-d-sep, discriminating paths, selection-bias semantics and full PAG completeness remain unresolved. FCI-lite output never writes JSPACE.

## 8. V13 longitudinal targeting

For `X -> A1 -> L1 -> A2 -> Y`, V13 estimates static two-timepoint regime risk through sequential logistic targeting.

The stage-2 pseudo-outcome retains **observed A1/L1 history** while setting only the second treatment to the target intervention. Its stage-2 clever covariate vanishes for histories incompatible with target A1. Stage 1 then learns the targeted pseudo-outcome over observed A1 before evaluating the target first intervention.

`STAGE2_PSEUDO_OUTCOME_PRESERVES_OBSERVED_A1_L1_BEFORE_STAGE1_INTERVENTION`.

`TWO_TIMEPOINT_SEQUENTIAL_TMLE != GENERAL_LONGITUDINAL_TMLE_THEOREM_OR_IDENTIFICATION_PROOF`.

Declared latent-confounding risk fails closed.

## 9. V13 dynamic two-timepoint policy value

`athena_dynamic_policy_value` admits deterministic binary or linear-threshold policies:

`A1=pi1(X)`;

`A2=pi2(X,A1,L1)`.

The policy is evaluated through the bounded parametric longitudinal model.

`DYNAMIC_GFORMULA_POLICY_VALUE != GENERAL_OFF_POLICY_CAUSAL_VALUE`.

`POLICY_VALUE != EXECUTION_AUTHORIZATION`.

## 10. V13 correlated robust resources

`athena_dro_resource_select` lifts independent Gaussian resource uncertainty to caller-declared covariance plus ellipsoidal mean ambiguity.

For subset `S`:

`robust_bound_r(S)=mu_r(S)+rho*||u_r(S)||_2+z_(1-alpha)*sqrt(1_S^T Sigma_r 1_S)`.

Small finite problems may receive an exhaustive-enumeration certificate. Large problems return an uncertified greedy plan.

`ELLIPSOIDAL_GAUSSIAN_ROBUST_PLAN != GENERAL_DISTRIBUTIONALLY_ROBUST_OPTIMIZATION`.

`MODEL_EXACTNESS != MODEL_CORRECTNESS`.

## 11. No-self-training / no-semantic-laundering

Forbidden implicit transitions include:

`QMC hyperbelief -> GP observation`;

`FITC prediction -> GP observation`;

`joint-design simulated measurement -> evidence`;

`FCI-lite candidate -> JSPACE edge`;

`sequential TMLE estimate -> identification proof`;

`dynamic policy value -> executed treatment`;

`robust resource plan -> resource expenditure`;

`science/model state -> Y1 authority`;

`caller CI/smoke packet -> trusted QUALIFIED`.

## 12. PROMOTION.2 trust boundary

MCP-visible `athena_promotion_evaluate` may produce `ATTESTED_READY` from exact-head caller-bound CI/smoke packets plus local gates.

Current `QUALIFIED` additionally requires a genuinely host-internal trusted verifier receipt binding:

- exact head;
- exact CI reference;
- exact smoke reference;
- verifier identity and verification reference.

The ordinary MCP input schema intentionally exposes no trusted-verifier field.

V13 **does not implement a caller-callable verifier**, because doing so would recreate self-qualification.

`UNRESOLVED_EXTERNAL_PROMOTION_VERIFIER` remains the correct state until a real host/control-plane bridge exists.

## 13. Live ABI / CI constitution

`ATHENA.RUNTIME.UNIFIED.8` retains compatibility with `.1` through `.7` and advertises `COLLECTIVE_ROBUST_V13`.

Release gate:

`syntax ∧ full-unit-suite ∧ critical-invariants ∧ dependent-smoke`.

The critical job must run V13 constructive, adversarial and unified-authority tests separately. The dependent subprocess smoke must cross representative V13 calls and PROMOTION.2 caller-bound readiness in one server process.

`CI_PASS != LIVE_TRUSTED_PROMRUN`.

## 14. External control plane

Repository settings, branch protection, tags, Releases, PR/merge state and the trusted promotion verifier are external control-plane state. OMEGA, SELFTEST, STARTUP, caller witness packets and GitHub Actions success do not imply those external states were changed or independently verified.

## 15. Successor boundary

Unresolved after V13 include exact/scalable continuous hyperparameter Bayes, optimized variational inducing inference, neural Bayesian world models, full FCI/RFCI/PAG completeness, calibrated structural posteriors, cross-fitted arbitrary-horizon longitudinal TMLE, stochastic/dynamic intervention policies beyond two times, doubly robust general off-policy policy value, robust continuous-state Bayes-adaptive control, general Wasserstein/f-divergence DRO, multistage stochastic resources, and a genuine trusted host promotion-verifier bridge.
