# CCR
`Σ=⟨kind,domain,verb,object,method,input,output,constraints⟩`; `CID=SHA256(Canon(Σ))`; canonical name `ATHENA::KIND::DOMAIN::VERB::OBJECT::METHOD::CID`. Search first; reuse identity; new CID only for changed functional contract.
