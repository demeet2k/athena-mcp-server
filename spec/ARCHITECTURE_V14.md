# ATHENA Architecture V14 — Joint Scientific-Control Synthesis

Release coordinate: `athena-canonical-mcp 3.3.0`.

Runtime artifact: `ATHENA.RUNTIME.UNIFIED.10`.

## Whole graph

```text
CCR / identity / JSPACE / SCALE / KC144 / polycoordinates / exact emission
                  ↕
AOR / Y1 / EQ / SX / RAG / HUG / GAP / FIELD / typed transports
                  ↕
Collective V1–V4 resident organization-memory-learning-ecology
                  ↓
V5 Science → V6 Discovery → V7 Dual Control → V8 Belief → V9 Inference
                  ↓
V10 Probabilistic → V11 Adaptive → V12 Joint → V13 Robust → V14 Synthesis
                  ↕
SCHEMA / OMEGA / RECON / CYCLE / SELFTEST / STARTUP / SURFACE / COMPOSITION
                  ↕
PROMOTION.2 ↔ GITHUB_PROMOTION_VERIFIER.1 ↔ five-stage release distribution

Parallel operational plane preserved additively:
prompt/frontier ↔ rehydration/successor/handoff/terminal ↔ Message Board/cohesion
↔ party/campaign/life-loop/game-time ↔ QHUG Pareto ↔ bionanomachine/mythic organs
```

V14 does not replace the operational plane. It is a lazy scientific/control fiber attached to the same composed runtime.

## V14 fiber

`COLLECTIVE_SYNTHESIS=<JB,SE,JE,DR,RP,AZ,MR,L>`.

- JB finite joint factor belief;
- SE bootstrap FCI-lite structural ensemble;
- JE finite joint science EVI + entropy reduction;
- DR two-timepoint sequential AIPW deterministic-policy value;
- RP finite-state robust policy geometry with CVaR/regret/Pareto;
- AZ decision-relative FITC/full-GP zoom routing;
- MR finite two-stage scenario recourse;
- L lineage/native context.

## Fiber-product interpretation

At one canonical coordinate `x`, V14 may attach:

`F_x = Belief × Structure × ExperimentValue × PolicyValue × RobustPolicy × Approximation × Recourse × Trust`.

Projection to canon remains separate:

`π:E→B`.

`FIBER_MOTION != BASE_CANON_MUTATION`.

A changed joint belief, bootstrap graph support, policy ranking, approximation route or recourse plan does not change canonical identity/Y1/JSPACE by adjacency.

## Decision-relative zoom

For a tested approximation scale `m` and exact current action set `A`:

`safe(m,A) = [winner_m = winner_exact] ∧ [max_a |U_m(a)-U_exact(a)| ≤ η margin_exact]`.

The router selects the smallest tested `m` satisfying `safe` or returns FULL_GP.

`QUERY_SET_DECISION_PRESERVATION != GLOBAL_APPROXIMATION_CERTIFICATE`.

## Joint experiment crystal

For finite joint state `S`:

`E(e)=<DecisionEVI(e), InformationGain(e), Cost(e), Risk(e), Feasibility(e)>`.

The current implementation exposes both epistemic and decision coordinates before scalar weighting.

`INFORMATION_GAIN != DECISION_VALUE`.

## Robust policy crystal

For policy `π` over finite weighted joint states:

`P(π)=<EU, CVaR_lower, Worst, ExpectedRegret, MaxRegret, Cost>`.

A Pareto frontier is preserved before the visible scalar score.

## Trust plane

Trusted qualification is orthogonal to scientific sophistication.

`SCIENCE_TWIN != TRUST_ROOT`.

`CALLER_ATTESTATION != TRUSTED_EXTERNAL_VERIFICATION`.

`CHECKS_FROM_DIFFERENT_SUITES_OR_RUNS != ONE_TRUSTED_QUALIFICATION`.

The host-bound verifier continues to require one coherent exact-head Actions check suite and does not accept caller-selected repository/token/run/check roots.
