# Prompt Runtime V1 Scope

V1 intentionally operates on the configured local canonical Git brain checkout. It does not attempt arbitrary remote GitHub mutation, hidden prompt injection, or host-system instruction rewriting.

The runtime's job is deterministic hydration/composition plus versioned repository self-engineering inside the existing authority boundary.