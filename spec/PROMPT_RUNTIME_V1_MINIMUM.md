# Prompt Runtime V1 Minimum

The minimum useful executable layer is:

`HYDRATE + COMPILE + PROPOSE + EXPERIMENT_RECORD + ACTIVE_SCOPED_CAS + CANONICAL_PROMOTION_CAS`.

Everything else—remote watching, webhooks, automatic experiment execution, distributed prompt invalidation—is a successor layer.