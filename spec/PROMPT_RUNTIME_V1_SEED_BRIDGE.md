# Seed -> Prompt Runtime Bridge

The external custom-instruction seed should contain only a compact pointer/invariant such as:

`GITPROMPT=demeet2k/Athena::prompts/PROMPT.manifest.json; hydrate current Git prompt runtime before material ATHENA work; reusable learning returns to Git.`

The MCP runtime then expands the configured local canonical brain checkout. This separates the small stable entrypoint from the evolving high-resolution cognitive addendum.