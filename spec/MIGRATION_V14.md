# ATHENA Migration V14 — UNIFIED.9 → UNIFIED.10

This migration begins from the live post-Ω13 organism, not from the historical Ω13 merge snapshot.

Observed start before Ω14 development:

- public `master=975d68c04b113e2b02899406216c4f327621f5f8`;
- public architecture `ATHENA.RUNTIME.UNIFIED.9`;
- package `athena-canonical-mcp 3.2.0`;
- private brain had independently advanced beyond the Ω13 V3.5 merge while its manifest still pinned the older Ω13 runtime head;
- host-bound `GITHUB_PROMOTION_VERIFIER.1` and five-stage release qualification already existed.

The public repository was hundreds of commits beyond Ω13 and contained prompt/frontier, rehydration/successor/handoff/terminal, Message Board/cohesion, campaign/life-loop/game-time, bionanomachine/mythic, release-distribution and trusted-verifier work. Ω14 therefore branched from current `master` and preserved that ancestry.

`NEW_SUCCESSOR != REPLAY_OLD_BASE`.

## Additive release identity

The historical v3.2 package registration body remains in `_init_v32_legacy.py`.

V14 installs current identity after that body:

`3.2.0 historical registration → additive current 3.3.0 overlay`.

This follows the existing additive installation model used by Message Board/rehydration organs rather than rewriting their registration history.

## Surface migration

V14 tool schemas extend the existing V12→V13 successor membrane. `SURFACE.2` is repaired to give V14 a dedicated group instead of counting transitive V14 names as V13.

New resource:

`athena://collective/v14`.

It is composed through the existing runtime-integrity resource lane so the giant post-Ω13 dispatcher does not need destructive replacement.

## Manifest migration

`ATHENA.RUNTIME.UNIFIED.9 -> ATHENA.RUNTIME.UNIFIED.10`.

Compatibility retains `.1` through `.9`.

New layer:

`COLLECTIVE_SYNTHESIS_V14`.

Cycle/navigation advance to `COLLECTIVE(V1-V14)` while `GITHUB_PROMOTION_VERIFIER.1` remains an independent trust layer.

## Joint-belief migration

V14 introduces bounded caller-declared finite factor products. No previous finite belief, GP hyperposterior, graph ensemble or causal estimate is automatically converted into a V14 joint state. A joint state exists only when the caller supplies explicit axes and compatibility/likelihood contracts.

`OLD_MODEL_STATE != IMPLICIT_JOINT_POSTERIOR`.

## Structural migration

V13 FCI-lite candidates remain individual observational hypotheses. V14 bootstrap ensembles resample data and rerun that bounded algorithm. Historical graph outputs do not gain posterior probability merely because bootstrap support now exists.

`BOOTSTRAP_GRAPH_FREQUENCY != CAUSAL_POSTERIOR`.

## Longitudinal migration

V13 sequential TMLE remains a static-regime targeted estimator. V14 sequential AIPW policy value is a separate estimator for deterministic dynamic policies and explicitly reports `cross_fitted=false`.

Both preserve observed first-stage history before later intervention evaluation.

`ESTIMATOR_SUCCESSION != RETROACTIVE_REINTERPRETATION`.

## Approximation migration

V13 FITC returns query-level error against the exact bounded GP. V14 turns that witness into a decision-relative routing condition over an explicitly supplied action/query set.

No historical FITC error becomes a global fidelity theorem.

`QUERY_SET_DECISION_PRESERVATION != GLOBAL_APPROXIMATION_CERTIFICATE`.

## Resource migration

V13 robust resource selection is one-stage correlated-Gaussian/ellipsoidal-mean planning. V14 finite two-stage recourse is a separate scenario model with deterministic budgets and bounded recourse options.

`FINITE_TWO_STAGE_SCENARIO_RECOURSE != GENERAL_MULTISTAGE_STOCHASTIC_PROGRAM`.

## Promotion migration

Ω13 originally left an external verifier unresolved. The post-Ω13 organism subsequently implemented `GITHUB_PROMOTION_VERIFIER.1`; Ω14 inherits that stronger trust substrate.

However:

`VERIFIER_IMPLEMENTATION_EXISTS != THIS_HEAD_IS_QUALIFIED`.

Each candidate release still needs one actual host-bound exact-head receipt created after syntax/unit/critical/smoke all pass in one coherent Actions suite. Old receipts are evidence only for the bytes/head they bind.

## Release distribution migration

V3.2 release files remain replayable historical distribution contracts.

V3.3 adds parallel `release/v3.3.0.*` and a V3.3 release workflow instead of rewriting the V3.2 recipe. The current distribution must still pass:

`syntax ∧ unit ∧ critical-invariants ∧ smoke -> promotion-qualification -> package-readiness`.

`OLD_RELEASE_RECEIPT != NEW_RELEASE_EVIDENCE`.
