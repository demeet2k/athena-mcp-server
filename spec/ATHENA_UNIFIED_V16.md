# ATHENA UNIFIED V16 — UNIFIED.12 COMPOSITION CONTRACT

## Candidate identity

Package candidate:

`athena-canonical-mcp@3.5.0`.

Runtime manifest candidate:

`ATHENA.RUNTIME.UNIFIED.12`.

Collective frontier candidate:

`COLLECTIVE_GENERALIZED_V16`.

Coordinate:

`COLLECTIVE_GENERALIZED=<OG,MH,GM,EF,NR,L>`.

Base runtime lineage:

`ATHENA.RUNTIME.UNIFIED.11 / COLLECTIVE_CALIBRATED_V15`.

## Additive composition

V16 is installed after V15 in the existing package namespace and same `Server` root.

It does not create a second service root and it does not replace inherited runtime organs.

The V16 dispatcher owns only:

- `athena_ordered_dag_posterior`;
- `athena_longitudinal_dr_multistage_crossfit`;
- `athena_gaussian_mixture_update`;
- `athena_approx_error_field`;
- `athena_coupled_model_robust_policy`.

All inherited tool names remain on their existing V1–V15 dispatch chain.

`V16_TOOL_SET ∩ INHERITED_TOOL_SET = ∅` is a required regression invariant.

## Manifest composition

The current manifest candidate must satisfy:

- artifact = `ATHENA.RUNTIME.UNIFIED.12`;
- artifact compatibility includes `ATHENA.RUNTIME.UNIFIED.11`;
- `COLLECTIVE_CALIBRATED_V15` remains present;
- `COLLECTIVE_GENERALIZED_V16` appears after V15 and before `GITHUB_PROMOTION_VERIFIER.1`;
- `organs.collective_v16.coordinate` equals `COLLECTIVE_GENERALIZED=<OG,MH,GM,EF,NR,L>`;
- `collective_generalized.authority` remains `MODEL_SCIENCE_TWIN_AND_PLAN_ONLY`;
- cycle/navigation advance to V1–V16;
- every V16 claim-ceiling law is present in manifest invariants.

## Resource composition

New resource:

`athena://collective/v16`.

The resource is additive. `athena://collective/v15`, deployment resources, promotion resources and all other inherited resources must remain present.

The V16 resource boundary must explicitly state that outputs do not mutate:

Y1 authority;
canonical JSPACE;
empirical observations;
execution;
deployment;
release publication;
trusted promotion.

## V16 bounded operator membrane

### OG

Exact finite posterior only inside the caller-order-constrained linear-Gaussian/BIC finite DAG family.

### MH

Cross-fitted sequential DR only for at most six binary treatment stages under caller-declared decision-time histories.

### GM

Exact finite Gaussian-mixture update only under the supplied mixture and shared linear-Gaussian observation.

### EF

RBF error-field prediction plus held-out residual quantile; not distribution-free conformal coverage or a global regularity theorem.

### NR

Exact supplied-policy evaluation across a finite complete-model family with one model fixed over the whole horizon; not general non-rectangular DRO optimization.

## Inherited operational fibres

The following are separate typed surfaces and must remain available after V16 installation:

- PROMPT / FRONTIER;
- REHYDRATION / successor / handoff / terminal;
- Message Board / Cohesion;
- Party / Campaign / Life / Game-Time;
- QHUG;
- bionano / mythic / symbolic / MCK;
- Deployment.2;
- release/distribution;
- PROMOTION.2 / GITHUB_PROMOTION_VERIFIER.1;
- Source/RETURN.

`COLLECTIVE_GENERALIZED != WHOLE_ORGANISM`.

## Public manifest holonomy

At release qualification, equivalent runtime manifest paths must resolve the same current V16 identity and invariants.

Required paths include the live tool/manifest response, `athena://runtime/unified-manifest`, and `athena://manifest` where currently registered.

`MODULE_ATTRIBUTE_ADVANCE != IMPORTED_VALUE_SNAPSHOT_ADVANCE`.

`MODULE_FUNCTION_REPLACEMENT != IMPORTED_FUNCTION_SNAPSHOT_ADVANCE`.

`SOURCE_LIST_MUTATION != COPIED_RESOURCE_REGISTRY_MUTATION`.

## Package/release identity

The branch code already declares package candidate 3.5.0, but source package metadata and release-distribution charts must migrate before integration.

Required equality at V16 release candidate time:

`pyproject.version == athena_mcp.__version__ == protocol.SERVER_INFO.version == Server.initialize.serverInfo.version == release_manifest.version == 3.5.0`.

Likewise:

`unified_manifest.artifact == release_manifest.runtime.manifest == ATHENA.RUNTIME.UNIFIED.12`.

Until those charts exist and pass:

`V16_PACKAGE_RELEASE_IDENTITY = HOLD`.

## Proof-selection membrane

V16 qualification must add real selectors for:

- `test_collective_v16.py`;
- `test_collective_v16_adversarial.py`;
- `test_collective_v16_unified.py`.

`ZERO_TEST_SELECTION != PROOF`.

A branch is not V16-qualified merely because inherited V15 tests remain green.

## Promotion / release boundary

V16 branch success may establish a candidate exact-head qualification only after syntax, full unit corpus, complete critical-invariants including V16, smoke and host-bound promotion qualification pass in one coherent suite.

That qualification remains exact-head scoped.

`BRANCH_GREEN != MASTER_INTEGRATED`.

`MASTER_INTEGRATED != RELEASE_PUBLISHED`.

`RELEASE_PUBLISHED != DEPLOYMENT`.

`QUALIFIED_RUNTIME != WORLD_TRUTH`.
