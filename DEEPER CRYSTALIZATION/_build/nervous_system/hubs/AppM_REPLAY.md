# AppM REPLAY

Role: replay kernel, verifier discipline, deterministic re-checks, and certificate closure.

Use when:
- a claim must be replayed
- a deterministic route is verified
- a certificate is attached
