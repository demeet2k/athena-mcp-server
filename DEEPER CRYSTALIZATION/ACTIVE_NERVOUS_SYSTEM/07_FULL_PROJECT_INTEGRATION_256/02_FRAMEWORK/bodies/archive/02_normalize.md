# Archive Normalize

## Role

ZIP-backed framework trees and historical payloads that remain hidden from ordinary browsing.

## Targets

- archive_atlas.json
- archive_manifest.json
- MATH framework archives

## 4 x 4 matrix

## File scale

- `archive.normalize.file.seed`: Use `normalize` on the single source artifact until it can define the object and create the first admissible representation.
- `archive.normalize.file.link`: Use `normalize` on the single source artifact until it can connect the object to parents, peers, and downstream surfaces.
- `archive.normalize.file.prove`: Use `normalize` on the single source artifact until it can add tests, receipts, witnesses, or quantitative closure.
- `archive.normalize.file.publish`: Use `normalize` on the single source artifact until it can promote the object into canonical operating surfaces.

## Folder scale

- `archive.normalize.folder.seed`: Use `normalize` on the family-level cluster until it can define the object and create the first admissible representation.
- `archive.normalize.folder.link`: Use `normalize` on the family-level cluster until it can connect the object to parents, peers, and downstream surfaces.
- `archive.normalize.folder.prove`: Use `normalize` on the family-level cluster until it can add tests, receipts, witnesses, or quantitative closure.
- `archive.normalize.folder.publish`: Use `normalize` on the family-level cluster until it can promote the object into canonical operating surfaces.

## Framework scale

- `archive.normalize.framework.seed`: Use `normalize` on the multi-folder subsystem until it can define the object and create the first admissible representation.
- `archive.normalize.framework.link`: Use `normalize` on the multi-folder subsystem until it can connect the object to parents, peers, and downstream surfaces.
- `archive.normalize.framework.prove`: Use `normalize` on the multi-folder subsystem until it can add tests, receipts, witnesses, or quantitative closure.
- `archive.normalize.framework.publish`: Use `normalize` on the multi-folder subsystem until it can promote the object into canonical operating surfaces.

## Ecosystem scale

- `archive.normalize.ecosystem.seed`: Use `normalize` on the whole-workspace coordination layer until it can define the object and create the first admissible representation.
- `archive.normalize.ecosystem.link`: Use `normalize` on the whole-workspace coordination layer until it can connect the object to parents, peers, and downstream surfaces.
- `archive.normalize.ecosystem.prove`: Use `normalize` on the whole-workspace coordination layer until it can add tests, receipts, witnesses, or quantitative closure.
- `archive.normalize.ecosystem.publish`: Use `normalize` on the whole-workspace coordination layer until it can promote the object into canonical operating surfaces.
