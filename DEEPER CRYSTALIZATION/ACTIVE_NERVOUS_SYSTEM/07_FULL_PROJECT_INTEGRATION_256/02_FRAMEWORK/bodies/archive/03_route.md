# Archive Route

## Role

ZIP-backed framework trees and historical payloads that remain hidden from ordinary browsing.

## Targets

- archive_atlas.json
- archive_manifest.json
- MATH framework archives

## 4 x 4 matrix

## File scale

- `archive.route.file.seed`: Use `route` on the single source artifact until it can define the object and create the first admissible representation.
- `archive.route.file.link`: Use `route` on the single source artifact until it can connect the object to parents, peers, and downstream surfaces.
- `archive.route.file.prove`: Use `route` on the single source artifact until it can add tests, receipts, witnesses, or quantitative closure.
- `archive.route.file.publish`: Use `route` on the single source artifact until it can promote the object into canonical operating surfaces.

## Folder scale

- `archive.route.folder.seed`: Use `route` on the family-level cluster until it can define the object and create the first admissible representation.
- `archive.route.folder.link`: Use `route` on the family-level cluster until it can connect the object to parents, peers, and downstream surfaces.
- `archive.route.folder.prove`: Use `route` on the family-level cluster until it can add tests, receipts, witnesses, or quantitative closure.
- `archive.route.folder.publish`: Use `route` on the family-level cluster until it can promote the object into canonical operating surfaces.

## Framework scale

- `archive.route.framework.seed`: Use `route` on the multi-folder subsystem until it can define the object and create the first admissible representation.
- `archive.route.framework.link`: Use `route` on the multi-folder subsystem until it can connect the object to parents, peers, and downstream surfaces.
- `archive.route.framework.prove`: Use `route` on the multi-folder subsystem until it can add tests, receipts, witnesses, or quantitative closure.
- `archive.route.framework.publish`: Use `route` on the multi-folder subsystem until it can promote the object into canonical operating surfaces.

## Ecosystem scale

- `archive.route.ecosystem.seed`: Use `route` on the whole-workspace coordination layer until it can define the object and create the first admissible representation.
- `archive.route.ecosystem.link`: Use `route` on the whole-workspace coordination layer until it can connect the object to parents, peers, and downstream surfaces.
- `archive.route.ecosystem.prove`: Use `route` on the whole-workspace coordination layer until it can add tests, receipts, witnesses, or quantitative closure.
- `archive.route.ecosystem.publish`: Use `route` on the whole-workspace coordination layer until it can promote the object into canonical operating surfaces.
