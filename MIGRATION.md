# ATHENA Unified Migration Law — v3.1 / Collective V1–V12

This repository has passed through historical active-tree reset, AOR×Collective constitutional braid, parallel Collective successor development, explicit ancestry repair, repository-brain consistency repair, PROMOTION.2 trust-boundary hardening, Collective V12 integration, and host-bound GitHub qualification. Migration therefore distinguishes **content**, **runtime integration**, **release identity**, **verification**, **authority**, **attestation**, **trusted qualification**, **control-plane state**, and **Git ancestry**.

`UNKNOWN / LEGACY != GARBAGE`

`PRUNE / HIBERNATE != ERASE`

`OLD API NAME != PERMANENT AUTHORITY`

`FILE EXISTS != ORGAN INTEGRATED`

`CONTENT COPIED != LINEAGE MERGED`

`GREEN OLD HEAD != QUALIFIED NEW HEAD`

`CALLER_ATTESTATION != TRUSTED_EXTERNAL_VERIFICATION`

`ATTESTED_READY != QUALIFIED`

`FOUR_GATE_PASS != LIVE TRUSTED PROMRUN`

`CHECKS_FROM_DIFFERENT_RUNS_OR_SUITES != ONE_TRUSTED_QUALIFICATION`

`RUNTIME STATE != GITHUB ADMINISTRATIVE STATE`

## 1. Current executable target

Current release identity:

`athena-canonical-mcp 3.1.0`

Live runtime brain:

`ATHENA.RUNTIME.UNIFIED.8`

One composed runtime contains:

- base CCR/JSPACE/SCALE/KC144/polycoordinate/crystal/Git substrate;
- Collective V1 + Growth V1 + Memory V2 + Learning V3 + Ecology V4;
- lazy Science V5 + Discovery V6 + Dual-Control V7 + Belief V8 + Continuous Inference V9 + Probabilistic V10 + Adaptive V11 + Joint V12;
- AOR.3 + branch evolution + Y1 authority;
- EQ.1 / SX.1 / RAG.1 / HUG.ABI.1 / GAP.1 / FIELD.1;
- typed AOR×Collective transport;
- CYCLE.1;
- SCHEMA.2 / OMEGA.1 / RECON.1;
- SELFTEST.1 / STARTUP.1 / SURFACE.2 / COMPOSITION.2 / PROMOTION.2;
- `GITHUB_PROMOTION_VERIFIER.1`;
- exact final-emission verification.

`.7 → .8` is **governance/trust integration only**. Package semver remains 3.1.0 and the model frontier remains Collective V12. Do not invent V13 to name a control-plane repair.

Historical PROMOTION.1 receipts remain versioned/replayable but are no longer sufficient to describe the current qualification trust contract.

## 2. Historical reset and ancestry boundary

Pre-rebuild repository state remains in Git history. Legacy existence does not make a file canonical.

Legacy admission remains:

`DISCOVER → HASH → CLASSIFY → CCR SEARCH → OID/VID → SOURCE → KC144 → JSPACE → SCALE → TEST/EVIDENCE → STATUS`.

A newer green successor must preserve the current master ancestry rather than replacing master with an older otherwise-green snapshot. Concurrent master motion triggers reconciliation plus complete requalification; old-head green evidence cannot be transferred to a new commit.

## 3. Database migration — SCHEMA.2

Schema adoption remains additive and receipt-bearing. Unknown legacy tables/rows survive unless an explicitly authorized migration owns them. Future schema versions fail closed rather than silently downgrading.

V12 itself adds no new persistent SQL table: its hyperposterior/BMA/PAG/g-formula/chance-control results are read-only computation surfaces over existing state and caller inputs. `NO_NEW_TABLE != NO_NEW_CAPABILITY`.

PROMOTION.2 reuses the versioned `promotion_runs` ledger while storing version-specific certificate semantics so historical PROMOTION.1 receipts remain replayable under the evaluator that created them.

The GitHub qualification job may create a temporary qualification runtime DB and upload its receipt. That artifact is a real receipt for the exact CI head, but it is not represented as magically inserted into an unrelated long-lived runtime database.

## 4. Claim namespace / authority firewall

Canonical Y1 remains:

- `athena_claim_register`
- `athena_claim_state`
- `athena_claim_promote`
- `athena_claim_challenge`
- `athena_claim_resolve_canonical_challenge`.

Science-shadow replication/falsification remains:

- `athena_discovery_claim_register`
- `athena_discovery_claim_witness`
- `athena_discovery_claim_state`.

Migration law:

`SCIENCE_SHADOW --explicit witnessed evidence route--> Y1 consideration`

never:

`SCIENCE_SHADOW --implicit RPC alias--> Y1 mutation`.

V7–V12 evidence/model/control operators may consume or produce science-shadow/model state but cannot mutate Y1 by adjacency.

## 5. Introspection / observer-effect law

Canonical introspection remains non-self-metering. OMEGA, schema status/verify, SELFTEST, STARTUP, SURFACE, runtime manifest/MAXDEV, benchmark/Git status and reconstruction reads must not change the state they are inspecting merely by observation.

`OBSERVE(OMEGA) != MUTATE(OMEGA)`.

The GitHub verifier is an external-state observer plus explicit PROMOTION transaction. A failed external observation must not create a PROMRUN.

## 6. Runtime surface integration rule

A file is not a live organ merely because it exists. Integration requires:

1. tool schema advertised;
2. dispatcher route exists;
3. organ resident or explicitly classified lazy/host-bound;
4. resource surface exists where applicable;
5. SURFACE.2 requires discoverable tools;
6. COMPOSITION.2 correctly classifies resident versus lazy/host-bound state;
7. constructive/adversarial/unified regression witnesses exist;
8. CI has a dedicated critical stage when constitutionally important;
9. smoke crosses representative behavior;
10. manifest and OMEGA describe the runtime truthfully;
11. README/ARCHITECTURE/MIGRATION/current unified spec agree with the live artifact;
12. when a claim is specifically about an external verifier, a real external verification run must witness it before architectural promotion.

`test_documentation_consistency.py` makes repository-brain drift CI-visible.

## 7. V6 → V11 preserved migrations

The existing progression is preserved without reinterpretation:

- V7 adds bounded dual-control/extended causal/replication geometry;
- V8 adds finite model belief/EVI;
- V9 adds Gaussian-linear parameter belief, EVPI/EVSI and AIPW;
- V10 adds fixed-kernel exact GP, bounded PC-stable, binary TMLE, E-value, finite POMDP and externally labelled dependence calibration;
- V11 adds finite-grid GP hyperfit with observed-row CAS, GP decision EVSI, supplied-DAG latent ADMG projection, stacked nuisance TMLE, RR sensitivity surface, finite-model BAPOMDP and dependence intervals.

Historical states remain typed as the layer that generated them. No V10 GP point prediction is relabelled as V12 BMA prediction; no V11 hyperfit winner is automatically treated as a finite-grid posterior.

## 8. V11 → V12 — fixed winners to joint bounded uncertainty

V12 is a **joint-uncertainty extension**, not a license to promote model mixtures into truth.

### Finite GP hyperparameter posterior

V11 can rank/apply one finite-grid hyperparameter winner. V12 `athena_gp_hyperposterior` normalizes the visible candidate set:

`w_i ∝ prior_i * exp(log p(y|X,theta_i))`.

`FINITE_GRID_HYPERPOSTERIOR != CONTINUOUS_HYPERPARAMETER_BAYES`.

Existing V11 hyperfit receipts remain model-selection receipts and are not retroactively converted into posterior priors.

### Bayesian-model-averaged GP prediction

V12 `athena_gp_bma_predict` combines model means and retains within-model and between-model uncertainty:

`Var = E[Var(Y|M)] + Var(E[Y|M])`.

`BMA_GP_POSTERIOR != WORLD_TRUTH`.

A historical fixed-GP prediction remains a fixed-GP prediction.

### Deterministic subset GP approximation

`athena_gp_sparse_predict` selects a farthest-point subset of current GP observations and returns explicit query-level error relative to the exact bounded current GP reference.

`SUBSET_GP_APPROXIMATION != FULL_GP_POSTERIOR`.

Do not migrate this surface into a claim of sparse variational GP or inducing-point optimality.

### BMA GP decision EVSI

`athena_gp_bma_decision_evsi` hypothetically updates finite model weights and within-model GP action means under simulated measurements. It remains `DESIGN_ONLY`.

`BMA_GP_EVSI != OBSERVATION`.

No hypothetical sample becomes an observation or hyperparameter update.

### Bounded PAG candidate

V12 `athena_pag_candidate_discover` adds an observed-data structural hypothesis surface using bounded Gaussian CI tests, separation sets, collider arrowheads and limited endpoint propagation.

Existing V9 partial graphs, V10 PC-stable graphs and V11 supplied-DAG latent projections remain distinct objects.

`BOUNDED_PAG_CANDIDATE != FCI_RFCI_PAG_THEOREM`.

No call creates canonical JSPACE causal edges.

### Two-timepoint parametric g-formula

V12 adds a static two-timepoint binary treatment-regime estimator over `A1 → L1 → A2 → Y` with optional baseline covariates.

`TWO_TIMEPOINT_GFORMULA != LONGITUDINAL_TMLE_OR_IDENTIFICATION_PROOF`.

Sequential exchangeability, positivity, consistency and nuisance-model correctness remain explicit assumptions. Declared latent-confounding risk fails closed.

Historical cross-sectional TMLE/AIPW results are not reclassified as longitudinal policy values.

### Chance-constrained resource selection

V12 models candidate resource consumption using explicit mean/std packets and the assumption of independent Gaussian consumptions within each resource dimension.

Small finite candidate sets are exhaustively enumerated under one-sided Gaussian bounds:

`mu_r(S)+z_(1-alpha)sigma_r(S) <= B_r`.

Exact certificate:

`EXACT_ENUMERATION_UNDER_DECLARED_INDEPENDENT_GAUSSIAN_RESOURCE_MODEL`.

Above the exact threshold, fallback is explicitly greedy with no optimality certificate.

`GAUSSIAN_INDEPENDENT_CHANCE_CERTIFICATE != DISTRIBUTION_FREE_RESOURCE_GUARANTEE`.

No returned allocation executes resources by itself.

## 9. UNIFIED.7 → UNIFIED.8 — trusted qualification becomes executable

`.7` already contained PROMOTION.2’s semantic split:

`CALLER_ATTESTED_READY != TRUSTED_QUALIFIED`.

But the trusted bridge was unresolved. `.8` adds `GITHUB_PROMOTION_VERIFIER.1` without modifying the V12 model operators.

### Host-controlled trust root

`athena_promotion_verify_github` accepts only target `git_head`, actor/persistence controls and timeout. It does **not** accept repository, API URL, required check names, trusted app slug, GitHub run ID, token, CI reference, smoke reference or trusted-verifier receipt from the MCP caller.

Those trust roots are host state:

- `ATHENA_GITHUB_REPOSITORY` / `GITHUB_REPOSITORY`;
- HTTPS `ATHENA_GITHUB_API_URL` / `GITHUB_API_URL`;
- optional host token;
- host Actions run ID when present;
- trusted app fixed to `github-actions`;
- required checks fixed to `syntax`, `unit`, `critical-invariants`, `smoke`.

### Coherent-suite law

For target head `h`, the verifier independently fetches GitHub check-runs and requires one check-suite under the host-bound run where all four required checks are `completed/success` on `h`.

`SUCCESS(A,syntax) + SUCCESS(A,unit) + SUCCESS(B,critical) + SUCCESS(B,smoke) --X--> QUALIFIED` when `A != B`.

No cross-suite/run stitching is permitted.

### Failure law

`VERIFIER_UNAVAILABLE | VERIFIER_ERROR | NO_QUALIFYING_CHECK_SUITE → no PROMRUN`.

Failed verifier observations do not degrade into caller-attested qualification and do not persist a blocked/ready receipt merely because external truth could not be established.

### Success law

A coherent trusted suite produces CI/smoke/trusted-verifier references internally and calls PROMOTION.2. Local Server/SURFACE/COMPOSITION/configured-Git gates remain necessary. Only then may status become `QUALIFIED`.

## 10. OMEGA / manifest migration

OMEGA remains a versioned whole-state projection ABI while contents expand. V1–V4 are resident; V5–V12 are explicit lazy surfaces.

Current live architecture artifact:

`ATHENA.RUNTIME.UNIFIED.8`

Compatibility retains `.1` through `.7`.

`.8` adds verifier configuration/version and trust laws; it does not pretend GitHub secrets/tokens are part of OMEGA or public runtime state.

Model-layer additions must update OMEGA/manifest metadata, layers/invariants/unresolved boundary, MAXDEV law, SURFACE requirements, resource list/read handlers, tests, smoke, CI, and repository-brain documentation.

## 11. CI migration — readiness then qualification

The original four-job readiness gate remains:

`syntax ∧ unit ∧ critical-invariants ∧ dependent-smoke`.

Critical invariants include schema/restart, unknown legacy preservation, three-domain CAS, CYCLE, SURFACE/COMPOSITION, SELFTEST/STARTUP, manifest/MAXDEV, release metadata/RPC uniqueness, documentation consistency, **GitHub verifier adversarial tests**, V6↔Y1 firewall, V7, V8/V9, V10, V11, V12, AOR×Collective transport, and PROMOTION.2 caller/trusted separation.

A fifth dependent job runs only after readiness:

`promotion-qualification needs [syntax, unit, critical-invariants, smoke]`.

It checks out the exact PR head or push SHA, binds host repository/API/run identity, executes `scripts/qualify_github_head.py`, creates a real `QUALIFIED` PROMRUN in the qualification runtime DB, replays it, and uploads `promotion-receipt-<sha>`.

Short retries tolerate GitHub check-index propagation only. They do not weaken the coherent-suite law.

## 12. Parallel-lineage repair law

When AOR constitutional, Collective successor, or governance/trust branches evolve in parallel, successor code is braided into the constitutional substrate rather than replacing it.

`GREEN_n → FETCH(master) → DELTA → BRAID → READINESS → TRUSTED_QUALIFICATION → FETCH(master) → {stable: merge; moved: recurse}`.

Never reuse readiness or trusted qualification evidence from an older head.

The verifier implementation itself followed this law: earlier V11 work became stale after V12 moved master, so the verifier was reconstructed and requalified against the real V12 tree rather than force-merging stale bytes.

## 13. Git ancestry / merge law

Copying current-master files is not lineage reconciliation. A true braid preserves the relevant parent graph. Before final merge, confirm current master has not advanced incompatibly. If it has, reconcile current master into the candidate and rerun readiness **and trusted qualification**.

The final merged master SHA must independently pass syntax, unit, critical invariants, smoke, and `promotion-qualification`. Feature-branch/PR-head success does not qualify the master merge SHA.

## 14. Promotion semantics — PROMOTION.2 + GitHub verifier

MCP-visible `athena_promotion_evaluate` accepts the candidate head plus caller-supplied CI/smoke witness packets. If local Server/SURFACE/COMPOSITION/configured-Git gates and both packets agree on one successful head, the result is:

`ATTESTED_READY`.

Those packets are `CALLER_ATTESTED`; the runtime does not pretend it queried the provider.

`athena_promotion_verify_github` is a separate trusted route. A real `QUALIFIED` receipt means the runtime independently observed one coherent host-bound GitHub Actions suite on that exact head and bound the observed check references into PROMOTION.2.

Historical PROMOTION.1 receipts remain frozen and replayable under their V1 evaluator, but historical `QUALIFIED` is counted separately from current PROMOTION.2 trusted qualification.

`FOUR_GATE_PASS != LIVE TRUSTED PROMRUN`.

`PROMRUN_ON_HEAD_A != QUALIFICATION_OF_HEAD_B`.

## 15. External GitHub control-plane boundary

The verifier reads GitHub Actions/check state. It does not grant or imply repository administration.

Branch protection, repository description/settings, tags, Releases, PR state and merge state must still be queried/changed using explicit GitHub administrative capabilities. A trusted PROMRUN is not evidence those controls are configured.

`TRUSTED_RUNTIME_QUALIFICATION != GITHUB_ADMIN_HARDENING`.

Non-GitHub CI providers remain optional integrations requiring separately trusted host verifier implementations rather than caller assertions.
