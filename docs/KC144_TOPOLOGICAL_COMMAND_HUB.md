# KC144 Topological Command Hub

`KC144.TOPOLOGICAL.COMMAND.HUB.1.0.0` is the compositional top surface for the current AΩR × Collective runtime.

It does not claim that the organism is finished. It makes the unfinished state machine-readable, source-bound, testable, and navigable.

## Exact constitutional body

- 144 immutable GIDs in a 12 × 12 grid.
- Eight nonuniform constitutional bands: `H6 + X16 + BR21 + F37 + IC10 + KC15 + KC27 + SSN12 = 144`.
- Sixteen explicit coordinate-system records, including GID, grid, band-local, legacy `S3 × P4 × C12`, `M12 × M12`, wheel, mirror, D4, native subcrystal, and open polyatlas fibres.
- Every seat exposes exact round-trip coordinates, universal operation column, mirror, wheel angle, D4 images, and band-native coordinate.

## Executable graph atlas

The candidate generates and validates:

| Graph | Exact records | Boundary |
|---|---:|---|
| `P12 □ P12` physical grid | 264 | physical adjacency only |
| `C144` radial ring | 144 | directed coordinate succession |
| mirror involution | 72 | paired view only |
| `P7 □ C3` BR21 native | 39 | local metabolism |
| nonempty `B4` KC15 cover | 28 | support inclusion |
| `P3 □ P3 □ P3` KC27 native | 54 | no-wrap ternary navigation |
| compiler navigation graph | 690 declared | cardinality indexed; unavailable records are never fabricated |

## Giant fibre inventory

Static organs, source datasets, mathematics, coordinates, graphs, source harnesses/skills, transports, and IC10 gates are joined with every discovered MCP tool and resource. Dynamic tools/resources are deterministically seated in F37 without replacing GID identity.

Source bodies remain in Google Drive or their exact Git commits. Git stores source locators, role/state, and digests—not copied private corpus bodies.

## Structural crystal versus runtime truth

The structural crystal remains digest-bound to source snapshot `6b643134ee26ce117c2b548b5a89edf5cec55934`.

The runtime overlay is independently bound to active unified parent `10f1dc39ffc6066ea00f880ef522050394fd5e3a`. It derives liveness from complete tool **and** resource requirements rather than branch names or source-file presence.

Live on the active parent:

- Git ledger, CCR, JSPACE, SCALE, KC144, polycoordinates, transforms, holonomy, crystals, emissions, sessions, Git CAS;
- Collective Runtime, Collective Growth, Collective Memory V2;
- AΩR core, branch lifecycle, Y.1 authority, successor robustness;
- EQ.1 witnessed equivalence;
- SX.1 bounded recursive extraction;
- RAG.1 decision-conditioned retrieval;
- HUG.ABI.1 fail-closed implementation registry and invocation ledger;
- GAP.1 witnessed directed reachability.

Open frontier:

- FIELD.1 candidate-field assembly;
- SURFACE.1 and COMPOSITION.1 certification;
- PROMOTION.1 exact-head persistent receipts;
- mechanized Collective↔AΩR transports;
- whole-organism migration and source-return certification.

The hub therefore emits:

`TOPOLOGY_PASS__RUNTIME_SURFACE_PASS__ORGANISM_INTEGRATION_PARTIAL__PROMOTION_HOLD`.

## Command surface

Ten tools expose status, manifest, seat, inventory, graph, route, datasets, communication, readiness, and deterministic validation. Eight resources mirror the control center. The package and console entrypoints launch `HubServer`, which delegates every mature pre-existing capability to the unified parent and owns only the KC144 hub namespace.

## Reproducibility

- The structural seed binds the manifest digest and structural validation receipt.
- `scripts/materialize_kc144_hub.py` materializes the complete crystal and generated edge records.
- Tests cover all coordinate round trips, D4 reversibility, graph cardinalities, routing, digest stability, dynamic seating, resource discovery, runtime-organ liveness, inherited dispatch, and fail-closed readiness.

## Terminal gate

`ATHENA_READY iff C ∧ I ∧ E ∧ P ∧ R ∧ V ∧ O ∧ M ∧ S ∧ X`.

Every gate must be exactly `PASS`; `PARTIAL`, `PASS_STRUCTURAL`, `SOURCE_INDEXED`, or `STAGED_SOURCE` cannot self-promote.
