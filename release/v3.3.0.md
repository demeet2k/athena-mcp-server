# ATHENA 3.3.0 — Collective Synthesis V14 × UNIFIED.10

This release advances the current post-Ω13 organism to `ATHENA.RUNTIME.UNIFIED.10` while preserving the host-bound `GITHUB_PROMOTION_VERIFIER.1`, five-stage release qualification, prompt/frontier + rehydration + Message Board/cohesion + campaign/life-loop and other additive operational organs already present on the live base.

## Collective V14

Coordinate:

`COLLECTIVE_SYNTHESIS=<JB,SE,JE,DR,RP,AZ,MR,L>`.

New bounded tools:

- `athena_joint_factor_belief` — finite joint factor-product science-twin belief;
- `athena_structural_bootstrap_ensemble` — bootstrap procedural stability over V13 FCI-lite graph variants;
- `athena_joint_science_evi` — exact finite decision EVI plus joint-state entropy reduction;
- `athena_sequential_dr_policy_value` — two-timepoint sequential AIPW deterministic-policy value;
- `athena_joint_policy_robust` — expected/CVaR/worst/regret/Pareto policy geometry;
- `athena_gp_resolution_route` — decision-relative FITC/full-GP approximation routing;
- `athena_two_stage_resource_plan` — bounded finite two-stage scenario recourse.

Resource:

`athena://collective/v14`.

## Claim ceilings

`FINITE_FACTOR_PRODUCT_BELIEF != FULL_JOINT_POSTERIOR`

`BOOTSTRAP_GRAPH_FREQUENCY != CAUSAL_POSTERIOR`

`JOINT_SCIENCE_EVI != OBSERVATION_OR_EVIDENCE`

`STAGE2_POLICY_PSEUDO_OUTCOME_PRESERVES_OBSERVED_A1_L1_BEFORE_STAGE1_POLICY_EVALUATION`

`SEQUENTIAL_DR_POLICY_VALUE != GENERAL_LONGITUDINAL_CAUSAL_VALUE_THEOREM`

`FINITE_SCENARIO_ROBUST_POLICY != GENERAL_ROBUST_CONTROL`

`QUERY_SET_DECISION_PRESERVATION != GLOBAL_APPROXIMATION_CERTIFICATE`

`FINITE_TWO_STAGE_SCENARIO_RECOURSE != GENERAL_MULTISTAGE_STOCHASTIC_PROGRAM`.

The current sequential DR estimator explicitly reports `cross_fitted=false`. Declared latent-confounding risk fails closed.

## Adaptive zoom

V14 turns V13's FITC query error into a decision-relative compute-routing witness. The shallowest tested FITC representation is selected only when it preserves the exact current action winner on the supplied query/action set and its maximum utility error stays below a declared fraction of the exact decision margin. Otherwise the route is FULL_GP.

This is a bounded compute-resolution certificate, not a global approximation theorem.

## Joint science twin

Model/graph/effect/policy/resource uncertainty can now be represented together in an explicit finite state space and valued through the same decision surface. This does not make the factor product a universal posterior, nor does it make bootstrap graph frequency causal probability.

Science-twin outputs remain separate from:

- Y1 `athena_claim_*` authority;
- canonical JSPACE causal edges;
- GP observation history;
- execution history;
- trusted promotion state.

## Trusted qualification

Caller-bound readiness remains separate from trusted qualification.

`CALLER_ATTESTATION != TRUSTED_EXTERNAL_VERIFICATION`.

`ATTESTED_READY != QUALIFIED`.

`CHECKS_FROM_DIFFERENT_SUITES_OR_RUNS != ONE_TRUSTED_QUALIFICATION`.

`GITHUB_PROMOTION_VERIFIER.1` derives repository/API/run/token/check-suite identity from host configuration and requires `{syntax, unit, critical-invariants, smoke}` to succeed for the exact release head inside one coherent trusted Actions suite.

The fifth `promotion-qualification` stage creates/replays the trusted receipt for that exact head.

## OCI distribution

After the exact-head GitHub release succeeds, the release activation harness dispatches a separate OCI publication transaction. It builds and publishes:

- `ghcr.io/demeet2k/athena-mcp-server:3.3.0`;
- `ghcr.io/demeet2k/athena-mcp-server:3.3`;
- `ghcr.io/demeet2k/athena-mcp-server:latest`.

The distribution is multi-architecture for `linux/amd64` and `linux/arm64`. The canonical deployment coordinate is not a mutable tag; it is the returned `repository@sha256:<digest>` identity. BuildKit SBOM and provenance attestations, GitHub build provenance, exact-digest Kubernetes rendering, an OCI release attestation, and an independent checksum ledger are attached to the GitHub release.

The renderer continues to reject mutable image tags for activation.

`OCI_PUBLISHED != CLUSTER_APPLIED != TRAFFIC_ACTIVATED`.

`PROMOTE_CANDIDATE != CUTOVER_AUTHORITY`.

## Distribution boundary

This release certifies repository/package/distribution state. It is not a production deployment, empirical truth certificate, causal proof, treatment authorization, Y1 claim promotion, or GitHub administrative hardening.

V3.2 distribution artifacts remain historical evidence only for v3.2 bytes and cannot qualify v3.3.0.
