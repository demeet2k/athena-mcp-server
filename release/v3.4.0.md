# ATHENA 3.4.0 — Collective Calibrated V15 × UNIFIED.11 × Deployment.2

ATHENA 3.4.0 advances the Collective scientific-control frontier from **Collective Synthesis V14** to **Collective Calibrated V15**, while preserving the separately typed operational organism and inheriting **ATHENA.DEPLOYMENT.2** from live master.

Current architecture: `ATHENA.RUNTIME.UNIFIED.11`.

Coordinate:

`COLLECTIVE_CALIBRATED=<SR,XT,XD,CJ,AT,MD,L>`.

## New calibrated science/control organs

`SR` adds weighted out-of-fold isotonic reliability calibration for structural bootstrap support using externally labelled correctness examples. Identical support coordinates are pooled before PAV so one semantic coordinate cannot acquire order-dependent fitted values.

`XT` adds cross-fitted bounded two-timepoint sequential logistic TMLE with explicit time-order validation. Stage-2 targeting preserves observed `A1,L1` before stage-1 intervention.

`XD` adds cross-fitted bounded two-timepoint sequential AIPW deterministic-policy value with a decision-time information firewall: `A1` policies may use baseline only; `A2` policies may use baseline plus observed `A1,L1` only.

`CJ` adds exact finite-dimensional multivariate-Gaussian conditioning for declared linear-Gaussian observations plus linear Gaussian action-value/CVaR propagation. Unknown coefficient coordinates and non-finite state are rejected rather than silently interpreted.

`AT` adds witness-consistent declared-Lipschitz approximation-error transport while keeping geometric nearest witness, tightest unrestricted envelope, and tightest radius-eligible transport witness as distinct coordinates. When a radius is declared but no witness lies inside it, the local certificate is explicitly absent (`null`); the unrestricted global envelope remains visible separately and cannot silently satisfy the locality constraint.

`MD` adds exact finite-horizon robust dynamic programming for supplied finite state/action state-action rectangular total-variation ambiguity models with strict finite state/action/transition coordinate validation.

## Scientific and numerical claim ceilings

`OUT_OF_FOLD_ISOTONIC_RELIABILITY != CAUSAL_GRAPH_POSTERIOR`.

`CALIBRATION_LABELS_REQUIRE_EXTERNAL_WITNESS`.

`IDENTICAL_CALIBRATION_COORDINATE != MULTIPLE_FITTED_VALUES`.

`CROSS_FITTED_TWO_TIMEPOINT_TMLE != GENERAL_LONGITUDINAL_TMLE_THEOREM`.

`STAGE2_PSEUDO_OUTCOME_PRESERVES_OBSERVED_A1_L1_BEFORE_STAGE1_INTERVENTION`.

`CROSS_FITTED_SEQUENTIAL_DR != GENERAL_OFF_POLICY_CAUSAL_VALUE`.

`DECISION_TIME_HISTORY != FULL_ROW_STATE`.

`CROSS_FITTING != IDENTIFICATION`.

`LINEAR_GAUSSIAN_UPDATE != GENERAL_CONTINUOUS_JOINT_BAYES`.

`GAUSSIAN_LINEAR_CONTROL != GENERAL_BELIEF_MDP`.

`UNKNOWN_COEFFICIENT != ZERO_COEFFICIENT`.

`NONFINITE_NUMERIC_STATE != MODEL_COORDINATE`.

`DECLARED_LIPSCHITZ_ERROR_ENVELOPE != EMPIRICAL_GLOBAL_ERROR_TRUTH`.

`GEOMETRIC_NEAREST_WITNESS != TIGHTEST_ERROR_ENVELOPE_WITNESS`.

`GLOBAL_ENVELOPE != RADIUS_ELIGIBLE_LOCAL_CERTIFICATE`.

`NO_RADIUS_ELIGIBLE_WITNESS != GLOBAL_FALLBACK_CERTIFICATE`.

`TRANSPORT_CERTIFICATE_CONDITIONAL_ON_LIPSCHITZ_BOUND`.

`RECTANGULAR_TV_ROBUST_MDP != GENERAL_MULTISTAGE_DRO`.

`UNKNOWN_STATE_COORDINATE != UNUSED_METADATA`.

`NONFINITE_TRANSITION != PROBABILITY_MODEL`.

`MODEL_ROBUSTNESS != REAL_WORLD_GUARANTEE`.

## Release-overlay and proof membranes

Ω15 integration exposed multiple import-time snapshots of one semantic runtime coordinate. The package/protocol value, dispatch-imported value, manifest function, copied resource lists, and derived URI sets are synchronized explicitly in the V15 release overlay.

`RELEASE_ATTRIBUTE_UPDATE != IMPORTED_VALUE_SNAPSHOT_UPDATE`.

`PROTOCOL_RESOURCE_ADVANCE != LIVE_INTEGRITY_RESOURCE_ADVANCE`.

`INTEGRITY_RESOURCE_ADVANCE != COPIED_AOR_RESOURCE_ADVANCE`.

`MODULE_ATTRIBUTE_ADVANCE != IMPORTED_FUNCTION_SNAPSHOT_ADVANCE`.

The V3.4 critical-invariant contract also resolves every `unittest discover -p` selector against a real repository test file. A successful process that selected no tests is not accepted as proof.

`ZERO_TEST_SELECTION != PROOF`.

`TEST_COMMAND_EXIT_0 != WITNESSED_ASSERTION_EXECUTION`.

## Deployment.2 braid

While Ω15 was in flight, live master advanced with Deployment.2. The Ω15 branch therefore records a true two-parent Git braid and composes:

`V14 -> Deployment.2 -> V15 release overlay`.

Deployment remains a separately typed external-control organ. Its manifest, validation, activation planning, canary assessment and receipt verification do not become Collective model state and do not prove that production infrastructure changed.

`COLLECTIVE_CALIBRATED != DEPLOYMENT_AUTHORITY != COORDINATION_AUTHORITY`.

## Trust

`GITHUB_PROMOTION_VERIFIER.1` remains host-bound. Current `QUALIFIED` status requires one coherent trusted Actions suite for one exact head containing successful `syntax`, `unit`, `critical-invariants`, and `smoke` checks, followed by host-bound `promotion-qualification`.

Caller-visible attestations remain capped at `ATTESTED_READY`.

`VERIFIER_IMPLEMENTED != HEAD_QUALIFIED`.

## Release lineage

V3.3 manifests, notes, regression and OCI/publication lane remain historical evidence for V3.3 bytes. V3.4 owns the current candidate distribution identity.

`OLD_RELEASE_RECEIPT != NEW_RELEASE_EVIDENCE`.

`HISTORICAL_PUBLICATION_AUTHORITY != CURRENT_RELEASE_IDENTITY`.

A V3.3 release attestation is not evidence for V3.4 bytes.

## Scope

This release certifies repository/package/distribution state. It is not a production deployment, empirical truth certificate, causal identification proof, treatment authorization, or GitHub administrative hardening.
