# ATHENA Canonical MCP v2.9 — AΩR × Collective V1–V10 Unified Runtime

ATHENA is an executable Git/MCP nervous substrate that combines canonical identity/versioning, typed JSPACE, SCALE, KC144/polycoordinates, exact visible-output crystallization, developmental AOR decision-making, persistent Y1 claim authority, collective organization/memory/learning, causal and probabilistic inference, bounded belief control, restart-safe whole-state projection, and exact-head promotion.

The runtime is deliberately **layered**. Score, popularity, model output, evidence, authority, execution, observation, and persistence are different types and are not silently collapsed into one scalar.

## Constitutional braid

`AOR = WHAT is developmentally eligible`

`COLLECTIVE = HOW scarce execution / science / inference / control capacity is organized`

`Y1 = canonical claim authority`

`EQ1 = witnessed identity collapse only`

`OMEGA = current accessible whole-state projection`

`CYCLE = resumable developmental metabolism`

`PROMOTION = exact-head local + external qualification`

Core firewalls:

- `UNKNOWN != 0`; `KNOWN != COMPARABLE`;
- `CONSENSUS != EVIDENCE`;
- `PHEROMONE / REUSE / POPULARITY != EVIDENCE != Y AUTHORITY`;
- `AUTHORITY != CONFIDENCE != TRUTH PROBABILITY`;
- `PLANNED / SIMULATED / DESIGNED != EXECUTED / OBSERVED / RESULT`;
- `ATTEMPTED_WRITE != VERIFIED_PERSISTENCE`;
- `REACHABILITY != LOGICAL_PROOF != CAUSAL_PROOF`;
- `POSTERIOR != TRUTH`; `EIG != EVIDENCE`; `ROLLOUT != EXECUTION`;
- `BELIEF_POSTERIOR != CANONICAL_TRUTH`;
- `MODEL_GRAPH != CANONICAL_JSPACE_GRAPH`;
- `semantic VID CAS != Git HEAD CAS != topology-version CAS`;
- `athena_claim_* != athena_discovery_claim_*`.

## Runtime cycle

`HYDRATE → RECONRUN/OMEGA → MEMORY → SX → RAG → HUG → GAP → FIELD → MEASURE/CALIBRATE → Y1/AOR → COLLECTIVE V1–V10 → EXECUTE → VERIFY → LEARN → SUCCESSOR → COMPLETE`

CYCLE.1 advances deterministic internal phases and halts at semantic boundaries instead of fabricating progress. Missing measurement, authority, HUG implementation, workers, executor, witness, repair, or control state becomes an explicit `WAITING_*` state.

## Authority and transaction domains

Three write domains are intentionally independent:

- semantic object/version state: expected-VID CAS;
- Git state: expected-Git-head CAS;
- Collective topology: expected-topology-version CAS.

A stale write in one domain must not mutate either of the other two. Topology→JSPACE uses a recovery saga; SQLite+Git are never represented as one atomic transaction. Semantic compensation never pretends to rewrite Git history.

## AOR developmental cortex

AOR.3 evaluates explicit candidates/residuals only after metric readiness, calibration, dependencies, authority, claimed-test/persistence, contradiction, fake, and unsupported gates are satisfied. UNKNOWN operands remain UNKNOWN. Pareto alternatives and robustness state are preserved instead of forcing arbitrary scalar collapse.

Supporting organs:

- **Y.1 authority** — `? → + → ! → #` with verified support, witnessed execution, and explicit canonicalization;
- **EQ.1** — UNKNOWN sameness preserves identity; collapse only witnessed contradiction-free equivalent components;
- **SX.1** — bounded extraction work contracts; planning is not execution;
- **RAG.1** — ranks only supplied/retrieved records; retrieval `source_authority` is not Y1 authority;
- **HUG.ABI.1** — exact `HUG(io,au,fx,lm,er,st)` ABI; unresolved semantic implementation fails closed;
- **GAP.1** — witnessed directed reachability, not theorem or causal closure;
- **FIELD.1** — deterministic residual-to-action assembler; generated work is UNMEASURED and conflicting explicit operands become CONFLICT;
- **AOR×Collective Transport.1** — typed crossings for attention, alarms, allocation, observed RGO, bridge economics, and repair without semantic laundering.

Resources include `athena://orchestration/law`, `athena://authority`, `athena://equivalence`, `athena://extraction`, `athena://retrieval`, `athena://hug`, `athena://gap`, `athena://field`, and `athena://aor-collective/transport`.

# Collective runtime ladder

Collective answers **HOW** scarce capacity is organized after developmental eligibility exists. V1–V4 are resident organizational layers; V5–V10 are lazy science/inference/control surfaces instantiated on tool/resource access.

## V1 — organization

HIVE/SWARM/PACK/FLOCK/HERD/POD geometry, worker width, role allocation, bounded-neighbor topology, reserve, evidence-sensitive quorum/cross-inhibition, advisory stigmergy, and homeostasis.

`MAX_GROWTH != MAX_ACTIVITY`

`MAX_INTEGRATION != MAX_CONNECTIVITY`

Resource: `athena://collective/runtime`.

## Growth V1 — metabolism

Demand-sensitive allocation, living-bridge economics, FISSION/FUSE/HOLD pressure, dependency alarm transport, and lineage-preserving lifecycle.

Resource: `athena://collective/growth`.

## V2 — persistent organizational memory

Database-backed pheromones, typed JSPACE invalidation, observed-RGO calibration, versioned topology CAS/rollback, and reusable failure antibodies.

Resource: `athena://collective/v2`.

## V3 — bounded empirical adaptation

Observed budget memory, rollbackable regularized policy, simulate-only counterfactual ranking, empirical elder state, antibody evolution, multiscale pheromones, and runtime metering.

Canonical introspection is non-self-metering so OMEGA/health reads do not alter the state they observe.

Resource: `athena://collective/v3`.

## V4 — uncertainty-aware experimental ecology

Contextual-UCB experiment selection, design-dependent credit, measured costs, shrinkage diffusion, bounded scheduling, explicit-transition rollouts, and topology→JSPACE recovery sagas.

`UCB != TRUTH`; `ASSOCIATION != CAUSATION`; `ROLLOUT != COMMIT`.

Resource: `athena://collective/v4`.

## V5 — causal experimental operating surface

Full-covariance contextual Bayesian prediction, retained pre-update calibration, expected-information-gain experiment design, interaction/delayed credit, learned transitions, finite-horizon scheduling, witness cells, learned regime geometry, finite Pareto frontiers, and projection compensation.

`POSTERIOR != TRUTH`; `CALIBRATION != MODEL_VALIDITY`; `EIG != EVIDENCE`; `TRANSITION_MODEL != WORLD`; `PARETO_FRONTIER != SINGLE_BEST`.

Resource: `athena://collective/v5`. Spec: `spec/COLLECTIVE_RUNTIME_V5.md`.

## V6 — active discovery + stochastic control

Degree-2 nonlinear Bayesian lift, empirical OOD pressure, finite factor experiment generation, conditional back-door identification, higher-order factorial contrasts, multivariate transition distributions, receding-horizon MPC, exact small finite schedule certificates, fail-closed witness capsules, interval-Pareto experiment selection, and persistent replication/falsification science shadows.

`NONLINEAR_BASIS != UNIVERSAL_INFERENCE`; `OOD_SCORE != FACTUAL_FALSEHOOD`; `MPC_PLAN != EXECUTION`; `BACKDOOR_SET != CAUSAL_TRUTH OUTSIDE THE SUPPLIED DAG`.

Resource: `athena://collective/v6`. Spec: `spec/COLLECTIVE_RUNTIME_V6.md`.

## V7 — dual control + conditional causal discovery

Model-conditional uncertainty decomposition, prequential residual bands, heuristic association skeletons, state-dependent transition regression, finite scenario/CVaR evaluation, bounded dual-control planning, supplied-DAG BACKDOOR/FRONTDOOR/INSTRUMENT checks, replication-independence geometry, and replication/falsifier design.

`UNCERTAINTY_PROXY != PHYSICAL_DECOMPOSITION`; `ASSOCIATION_SKELETON != CAUSAL_DAG`; `SCENARIO_TREE != OBSERVED_FUTURE`; `DUAL_CONTROL_PROXY_PLAN_ONLY != EXACT_BELIEF_STATE_CONTROL`; `REPLICATION_EFFECTIVE_N != FORMAL_INDEPENDENCE`.

Resource: `athena://collective/v7`. Unified operating spec: `spec/ATHENA_UNIFIED_V7.md`.

## V8 — finite belief-state decision layer

Persistent finite hypothesis beliefs, explicit observation-driven Bayes updates, expected value of information, belief-state action selection, simple linear causal-effect estimation under declared assumptions, bootstrap structural stability, contingent finite policies, and spectral evidence diversity.

`BELIEF_POSTERIOR != CANONICAL_TRUTH`; `EVI / DESIGN != RESULT`; `BELIEF_POLICY != EXECUTION`; `LINEAR_EFFECT_ESTIMATE != IDENTIFICATION_PROOF`; `BOOTSTRAP_EDGE_SUPPORT != CAUSAL_PROBABILITY`; `SPECTRAL_DIVERSITY != FORMAL_INDEPENDENCE`.

Resource: `athena://collective/v8`. Spec: `spec/COLLECTIVE_RUNTIME_V8.md`.

## V9 — continuous inference + causal robustness

Finite-dimensional Gaussian-linear belief state, Monte-Carlo EVPI/EVSI, multistage finite-belief planning, AIPW estimation under declared assumptions, leave-one-adjustment robustness probes, heuristic partial graphs, and caller/model-based evidence-dependence probabilities.

`GAUSSIAN_LINEAR_POSTERIOR != GENERAL_CONTINUOUS_BAYES`; `MONTE_CARLO_EVPI_EVSI != EXACT_ANALYTIC_VALUE`; `MULTISTAGE_FINITE_BELIEF_POLICY != GENERAL_POMDP`; `AIPW_ESTIMATE != IDENTIFICATION_PROOF`; `HEURISTIC_PARTIAL_GRAPH != FCI/PAG/CPDAG_THEOREM`.

Resource: `athena://collective/v9`. Spec: `spec/COLLECTIVE_RUNTIME_V9.md`.

## V10 — probabilistic world model + causal control

V10 adds a bounded zero-dependency probabilistic layer:

- exact small-data fixed-hyperparameter RBF Gaussian-process regression;
- bounded Gaussian PC-stable structural hypothesis search;
- binary-treatment/binary-outcome cross-fitted TMLE;
- standard risk-ratio E-value sensitivity metric;
- exhaustive finite-state finite-horizon POMDP tree search with exact certificate only after complete bounded enumeration;
- externally labelled logistic evidence-dependence calibration.

Constitutional boundaries:

`FIXED_KERNEL_GP != GENERAL_WORLD_TRUTH`

`GP_POSTERIOR != OBSERVATION`

`BOUNDED_PC_STABLE != FCI_OR_HIDDEN_CONFOUNDER_DISCOVERY`

`PC_GRAPH != CANONICAL_JSPACE_GRAPH`

`TMLE_ESTIMATE != IDENTIFICATION_PROOF`

`E_VALUE != UNIVERSAL_HIDDEN_CONFOUNDING_BOUND`

`FINITE_POMDP_CERTIFICATE != INFINITE_HORIZON_OR_REAL_WORLD_OPTIMALITY`

`LEARNED_DEPENDENCE_MODEL != FORMAL_INDEPENDENCE_PROOF`

Resource: `athena://collective/v10`. Spec: `spec/COLLECTIVE_RUNTIME_V10.md`.

# Claim namespace separation

Two claim systems coexist and must not alias:

- `athena_claim_*` — Y1 canonical authority;
- `athena_discovery_claim_*` — V6–V10 science-shadow replication/falsification evidence-navigation state.

A science-shadow state such as `REPLICATED_SUPPORT`, `FALSIFICATION_SIGNAL`, or `CONTESTED` cannot implicitly promote, demote, challenge, or canonicalize Y1. Any model/shadow output that should affect canonical authority must cross an explicit witnessed evidence/authority route.

# State foundation and restart continuity

- **SCHEMA.2** — additive migration ledger with table+column verification, future-schema downgrade block, and explicit RECONRUN column migration;
- **OMEGA.1** — digest-addressed current accessible state; V1–V4 resident state plus lazy V5–V10 model-surface metadata;
- **RECON.1** — persistent `RECONRUN=<task,consulted,expected,OMEGA,defects,digest>`;
- **CYCLE.1** — resumable fail-closed developmental metabolism;
- **SELFTEST.1** — read-only synthesis of surface/composition/schema/OMEGA/replay health;
- **STARTUP.1** — typed local readiness (`DEGRADED_*` versus `READY_LOCAL`);
- **SURFACE.2** — required tool/resource union across base, Collective V1–V10, AOR, state, and governance;
- **COMPOSITION.2** — verifies one composed Server and resident organs while correctly classifying V5–V10 as lazy surfaces;
- **PROMOTION.1** — exact-head qualification requiring local gates plus external CI and smoke attestations for the same head.

Unknown legacy tables/rows survive migration. Hibernation/pruning never means erasing required lineage.

# Exact visible output

`athena_finalize_output` crystallizes the body, derives the header, assembles the exact visible envelope, creates an emission manifestation, indexes visible lexemes, and returns bytes verifiable by `athena_verify_emission`.

Coordinate lists are navigation only. `LOOKUP != DERIVATION`; measured holonomy is promoted only for all-derivational routes.

# Live runtime brain

- `athena://manifest` and `athena://runtime/unified-manifest` expose `ATHENA.RUNTIME.UNIFIED.5`;
- `athena://runtime/maxdev` and `athena_maxdev_law` expose the V10 operational law;
- `athena://collective/v5` … `athena://collective/v10` expose lazy scientific/control layers with explicit epistemic boundaries;
- stable compatibility aliases such as `AOR_DECISION_CORTEX` and `braid_law` remain present.

# CI and promotion

The unified release gate is deliberately split:

`syntax ∧ full-unit-suite ∧ critical-invariants ∧ dependent-smoke`.

Critical invariants independently exercise:

- schema/restart and unknown legacy preservation;
- three-domain CAS isolation;
- whole CYCLE fail-closed behavior;
- SURFACE/COMPOSITION;
- SELFTEST/STARTUP;
- live manifest/MAXDEV;
- release metadata and RPC uniqueness;
- V6↔Y1 namespace firewall;
- V7 boundaries;
- V8/V9 belief/inference boundaries;
- V10 constructive + adversarial + unified authority/nonmutation boundaries;
- AOR×Collective transport;
- promotion predicate.

Smoke runs only after syntax, unit, and critical jobs pass and exercises one stdio process across V6 discovery, V7 dual control, V8 belief, V9 inference, V10 GP/POMDP, schema/SELFTEST, AOR, fail-closed CYCLE/HUG, and final crystal emission.

A green branch head is still not canonical promotion until PROMOTION.1 receives exact-head external CI and smoke attestations.

## Run

`python -m athena_mcp --db ./state/athena.db`

Package: `athena-canonical-mcp 2.9.0`  
MCP protocol revision: `2025-11-25`.
