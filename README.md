# ATHENA Canonical MCP v3.5 — AΩR × Collective V1–V16 × Operational Organism × Deployment.2

ATHENA is an executable Git/MCP developmental substrate. The current **candidate** runtime composes canonical identity/versioning, typed JSPACE, SCALE, KC144/polycoordinates, AOR/Y1 developmental routing, Collective V1–V16 science/control layers, restart-safe state foundation, prompt/frontier and rehydration loops, Message Board/cohesion/party coordination, campaign/life/game organs, bounded symbolic/mythic/bionano kernels, Deployment.2 external-control planning, versioned release distribution, and exact-head qualification through a host-bound GitHub verifier.

Current candidate coordinates:

- package: `athena-canonical-mcp 3.5.0`;
- live architecture candidate: `ATHENA.RUNTIME.UNIFIED.12`;
- runtime root: one composed `Server`;
- Collective ladder: V1–V16;
- current Collective coordinate: `COLLECTIVE_GENERALIZED=<OG,MH,GM,EF,NR,L>`;
- inherited V15 coordinate: `COLLECTIVE_CALIBRATED=<SR,XT,XD,CJ,AT,MD,L>`;
- deployment frontier: `ATHENA.DEPLOYMENT.2`;
- authority: Y1 `athena_claim_*`;
- science-shadow namespace: `athena_discovery_claim_*`;
- governance: `SCHEMA.2 / SELFTEST.1 / SURFACE.2 / COMPOSITION.2 / PROMOTION.2`;
- trusted verifier: `GITHUB_PROMOTION_VERIFIER.1`;
- trusted qualification rule: `syntax ∧ unit ∧ critical-invariants ∧ smoke → promotion-qualification`.

V16 is not yet canonical merely because this branch contains code. Integration, publication, deployment and empirical authority remain separately gated.

## Constitutional braid

`AOR = WHAT is developmentally eligible`.

`COLLECTIVE = HOW bounded science/inference/control capacity is organized`.

`Y1 = canonical claim authority`.

`OPERATIONAL ORGANISM = prompt/frontier + rehydration/successor/handoff + Message Board/cohesion/party + campaign/life/game + symbolic/mythic/bionano organs`.

`DEPLOYMENT.2 = separately typed external-control planning / validation / canary / receipt verification`.

`PROMOTION.2 = caller-bound readiness + separately trusted exact-head qualification`.

Core firewalls:

`UNKNOWN != 0`

`CONSENSUS != EVIDENCE`

`PREDICTION / POSTERIOR / PLAN != OBSERVATION / TRUTH / EXECUTION`

`MODEL_GRAPH != CANONICAL_JSPACE_GRAPH`

`CALLER_ATTESTATION != TRUSTED_EXTERNAL_VERIFICATION`

`ATTESTED_READY != QUALIFIED`

`COLLECTIVE_GENERALIZED != DEPLOYMENT_AUTHORITY != COORDINATION_AUTHORITY`.

## Runtime cycle

`HYDRATE → RECONRUN/OMEGA → MEMORY → SX → RAG → HUG → GAP → FIELD → MEASURE/CALIBRATE → Y1/AOR → COLLECTIVE(V1–V16) → AUTHORIZED EXECUTION → VERIFY → LEARN → SUCCESSOR → COMPLETE`.

CYCLE stops at typed missing prerequisites instead of fabricating execution, evidence, workers, tests or authority.

## V16 — bounded generalized scientific control

The canonical V3.7 brain names a broader Ω16 research pressure. The current implementation realizes only bounded, explicit subsets. The research title is not a capability certificate.

Resource: `athena://collective/v16`.

Specs: `spec/COLLECTIVE_RUNTIME_V16.md`, `spec/ATHENA_UNIFIED_V16.md`, `spec/ARCHITECTURE_V16.md`, `spec/MIGRATION_V16.md`.

### OG — order-constrained finite DAG posterior

`athena_ordered_dag_posterior` enumerates the finite DAG family consistent with a **caller-declared topological order** over 2–5 variables. Local weights use the implemented linear-Gaussian BIC score plus an independent edge prior. The returned posterior is exact only inside that bounded family/model.

Optional external calibration examples may map raw edge support through V15 isotonic reliability calibration.

`ORDER_CONSTRAINED_DAG_POSTERIOR != GENERAL_CAUSAL_GRAPH_POSTERIOR`.

`CALLER_ORDER != DISCOVERED_CAUSAL_ORDER`.

`CALIBRATED_RELIABILITY != CANONICAL_JSPACE_EDGE`.

### MH — bounded multistage cross-fitted sequential DR

`athena_longitudinal_dr_multistage_crossfit` supports 1–6 binary treatment stages under explicit caller-declared decision-time histories and supplied policies. Histories containing current/future treatment fields or the outcome fail closed.

`BOUNDED_MULTISTAGE_CROSS_FITTED_DR != ARBITRARY_HORIZON_LONGITUDINAL_THEOREM`.

`CALLER_DECLARED_HISTORY != VERIFIED_REAL_WORLD_CHRONOLOGY`.

`CROSS_FITTING != IDENTIFICATION`.

`SEQUENTIAL_DR_ESTIMATE != TREATMENT_AUTHORIZATION`.

### GM — finite Gaussian-mixture belief

`athena_gaussian_mixture_update` performs exact finite-mixture Bayes for a supplied 2–16 component Gaussian mixture under one shared finite linear-Gaussian observation.

Unknown coefficients, non-finite state, non-PSD covariance and invalid weights reject.

`FINITE_GAUSSIAN_MIXTURE != GENERAL_NON_GAUSSIAN_BAYES`.

`LINEAR_GAUSSIAN_OBSERVATION != GENERAL_LIKELIHOOD`.

`GAUSSIAN_MIXTURE_POSTERIOR != WORLD_TRUTH`.

### EF — learned approximation-error field

`athena_approx_error_field` learns a bounded RBF error surface from 30–96 explicit absolute-error witnesses, uses held-out residuals to construct a residual quantile, and reports nearest-witness support distance plus optional support-radius membership.

`CV_RESIDUAL_QUANTILE != DISTRIBUTION_FREE_ERROR_CERTIFICATE`.

`LEARNED_ERROR_FIELD != GLOBAL_LIPSCHITZ_CERTIFICATE`.

`IN_SUPPORT_RADIUS != WORLD_REGIME_MATCH`.

Unsupported/OOD geometry remains explicit.

### NR — coupled finite model-family robust policy evaluation

`athena_coupled_model_robust_policy` evaluates a supplied finite policy set against a supplied finite family of complete finite-state models. One model is held fixed over the entire 1–6 step horizon, so the ambiguity is globally coupled across state/time.

Returned comparisons include minimum model value, prior-weighted value, worst regret, weighted regret and per-model values.

`FINITE_FIXED_MODEL_FAMILY_POLICY_EVALUATION != GENERAL_NONRECTANGULAR_DRO_OPTIMIZATION`.

`SUPPLIED_POLICY_SET_EVALUATION != OPTIMIZATION_OVER_ALL_POLICIES`.

`FINITE_MODEL_FAMILY != REAL_WORLD_MODEL_SET`.

`ROBUST_POLICY_SCORE != EXECUTION_AUTHORITY`.

## V16 witness contract

V16 earns integration only if its own real witness files execute inside the canonical proof lattice:

- `tests/test_collective_v16.py` — bounded constructive behavior and deterministic positive cases;
- `tests/test_collective_v16_adversarial.py` — fail-closed/overclaim attacks;
- `tests/test_collective_v16_unified.py` — package/runtime/tool/resource/manifest/authority composition.

`ZERO_TEST_SELECTION != PROOF`.

Inherited V15 tests remaining green is necessary but not sufficient for V16 qualification.

## Release-overlay holonomy

V15 established the migration antibody that remains mandatory for V16:

`MULTIPLE_EQUIVALENT_ARCHITECTURE_CHARTS_REQUIRE_ALL_DECLARED_CHARTS_TO_MIGRATE`.

Package metadata, package namespace, protocol/dispatch `SERVER_INFO`, initialized server version, unified manifest, copied integrity/AOR resources and release metadata must agree before an exact head can qualify as V3.5/UNIFIED.12.

`MODULE_ATTRIBUTE_ADVANCE != IMPORTED_VALUE_SNAPSHOT_ADVANCE`.

`MODULE_FUNCTION_REPLACEMENT != IMPORTED_FUNCTION_SNAPSHOT_ADVANCE`.

`SOURCE_LIST_MUTATION != COPIED_RESOURCE_REGISTRY_MUTATION`.

## V15 — inherited calibrated scientific control

V15 remains callable and unchanged by name. Its coordinate is:

`COLLECTIVE_CALIBRATED=<SR,XT,XD,CJ,AT,MD,L>`.

It provides:

- `athena_structural_reliability_calibrate` — externally witnessed out-of-fold isotonic reliability;
- `athena_longitudinal_tmle_crossfit` — bounded history-safe cross-fitted two-timepoint TMLE;
- `athena_sequential_dr_policy_crossfit` — bounded history-safe cross-fitted sequential AIPW policy value;
- `athena_joint_gaussian_update` / `athena_joint_gaussian_control` — finite linear-Gaussian belief/control;
- `athena_approx_error_transport` — declared-Lipschitz local/global witness transport;
- `athena_multistage_tv_dro_plan` — finite-horizon rectangular-TV robust dynamic programming.

V15 firewalls remain inherited:

`OUT_OF_FOLD_ISOTONIC_RELIABILITY != CAUSAL_GRAPH_POSTERIOR`.

`CROSS_FITTED_TWO_TIMEPOINT_TMLE != GENERAL_LONGITUDINAL_TMLE_THEOREM`.

`DECISION_TIME_HISTORY != FULL_ROW_STATE`.

`LINEAR_GAUSSIAN_UPDATE != GENERAL_CONTINUOUS_JOINT_BAYES`.

`DECLARED_LIPSCHITZ_ERROR_ENVELOPE != EMPIRICAL_GLOBAL_ERROR_TRUTH`.

`RECTANGULAR_TV_ROBUST_MDP != GENERAL_MULTISTAGE_DRO`.

Resource: `athena://collective/v15`.

## Deployment.2 braid

Deployment remains separately typed external-control state. V16 installation must preserve the V15/Deployment.2 composition and all deployment tools/resources.

Deployment planning or validation is not proof that production infrastructure changed.

`COLLECTIVE_GENERALIZED != DEPLOYMENT_AUTHORITY`.

## Trusted promotion topology

`athena_promotion_evaluate` remains caller-bound and may reach `ATTESTED_READY` from exact-head caller packets.

`athena_promotion_verify_github(git_head)` is the host-bound route. One coherent Actions suite for one exact head must contain successful `syntax`, `unit`, `critical-invariants`, and `smoke` before `promotion-qualification` may mint a trusted receipt.

`CHECKS_FROM_DIFFERENT_SUITES_OR_RUNS != ONE_TRUSTED_QUALIFICATION`.

`BRANCH_RECEIPT != MERGED_HEAD_RECEIPT`.

`VERIFIER_IMPLEMENTED != HEAD_QUALIFIED`.

## Release distribution

V3.5 owns the candidate distribution identity for V16/UNIFIED.12.

Every `master` push is intended to receive exact-head V3.5 validation/package-readiness, while publication remains an explicit manual `workflow_dispatch` authority.

`AUTOMATIC_VALIDATION != AUTOMATIC_PUBLICATION`.

`MASTER_PUSH_PACKAGE_READY != RELEASE_PUBLISHED`.

`STALE_MASTER_VALIDATION != CURRENT_RELEASE_CANDIDATE`.

V3.4 manifests, notes, exact-head receipts and V15 cold-boot evidence remain historical evidence for V3.4/V15 only.

`OLD_RELEASE_RECEIPT != NEW_RELEASE_EVIDENCE`.

`V3_4_V15_COLD_BOOT_PASS != V3_5_V16_COLD_BOOT_PROOF`.

A V3.5 distribution receipt, if eventually produced, certifies exact repository/package/distribution state only. It is not empirical truth, causal proof, treatment/resource authority, production deployment or GitHub administrative authority.

## Historical architecture

Prior architectures are preserved rather than rewritten:

- V14: `spec/ARCHITECTURE_V14.md`, `spec/MIGRATION_V14.md`;
- V15: `spec/ARCHITECTURE_V15.md`, `spec/MIGRATION_V15.md`, `spec/ATHENA_UNIFIED_V15.md`, `spec/COLLECTIVE_RUNTIME_V15.md`.

Current V16 contracts live in the corresponding V16 spec files.

## Run

`python -m athena_mcp --db ./state/athena.db`

Package candidate: `athena-canonical-mcp 3.5.0`  
MCP protocol revision: `2025-11-25`.
